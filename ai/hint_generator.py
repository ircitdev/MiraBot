"""
Hint Generator.
Генерирует контекстные подсказки (кнопки быстрых ответов) на основе разговора.
"""

import re
from typing import List, Dict, Any, Optional
from dataclasses import dataclass
from loguru import logger


@dataclass
class Hint:
    """Подсказка для быстрого ответа."""
    text: str  # Текст на кнопке
    message: str  # Сообщение, которое будет отправлено
    priority: int = 0  # Приоритет (выше = важнее)


class HintGenerator:
    """Генератор контекстных подсказок."""

    # Максимум кнопок в ряду
    MAX_HINTS = 3

    # Подсказки по тегам/темам
    TOPIC_HINTS = {
        "topic:husband": [
            Hint("Расскажу подробнее", "Хочу рассказать подробнее о ситуации с мужем...", 5),
            Hint("А как у тебя?", "А как у тебя с Андреем? Бывает так же?", 3),
            Hint("Не знаю что делать", "Честно, не знаю что с этим делать...", 4),
        ],
        "topic:children": [
            Hint("Расскажу ещё", "Хочу ещё рассказать про детей...", 4),
            Hint("Я устала", "Я просто очень устала от всего этого...", 5),
            Hint("Нужен совет", "Мне нужен совет, как быть...", 3),
        ],
        "topic:self": [
            Hint("Хочу разобраться", "Хочу разобраться в себе...", 5),
            Hint("Чувствую пустоту", "Чувствую какую-то пустоту внутри...", 4),
            Hint("Мечтаю о...", "Знаешь, я мечтаю о...", 3),
        ],
        "topic:relatives": [
            Hint("Она меня бесит", "Честно? Она меня просто бесит...", 4),
            Hint("Не могу сказать нет", "Я не могу ей отказать, и это проблема...", 5),
            Hint("Муж не защищает", "Муж не встаёт на мою сторону...", 4),
        ],
        "topic:intimacy": [
            Hint("Мне не хватает", "Мне не хватает близости...", 5),
            Hint("Он не понимает", "Он просто не понимает, чего я хочу...", 4),
            Hint("Боюсь говорить", "Боюсь об этом говорить с ним...", 4),
        ],
        "topic:work": [
            Hint("Выгораю", "Кажется, я выгораю на работе...", 5),
            Hint("Хочу уйти", "Думаю уйти, но страшно...", 4),
            Hint("Не ценят", "Чувствую, что меня не ценят...", 4),
        ],
    }

    # Подсказки по эмоциональному состоянию
    EMOTION_HINTS = {
        "sad": [
            Hint("Мне грустно", "Мне просто грустно сейчас...", 5),
            Hint("Хочу поплакать", "Хочется просто поплакать...", 4),
            Hint("Обними меня", "Просто обними меня словами 💛", 6),
        ],
        "anxious": [
            Hint("Тревожно", "Мне очень тревожно...", 5),
            Hint("Не могу успокоиться", "Никак не могу успокоиться...", 4),
            Hint("Что со мной?", "Не понимаю, что со мной происходит...", 4),
        ],
        "angry": [
            Hint("Злюсь", "Я злюсь, и не могу остановиться...", 5),
            Hint("Хочу высказаться", "Дай выговориться, пожалуйста...", 6),
            Hint("Это несправедливо", "Это просто несправедливо!", 4),
        ],
        "tired": [
            Hint("Устала", "Я просто очень устала...", 6),
            Hint("Нет сил", "Нет сил ни на что...", 5),
            Hint("Хочу отдохнуть", "Мечтаю просто отдохнуть...", 4),
        ],
        "confused": [
            Hint("Запуталась", "Я совсем запуталась...", 5),
            Hint("Помоги понять", "Помоги мне разобраться...", 6),
            Hint("Не знаю как быть", "Не знаю, как правильно поступить...", 5),
        ],
    }

    # Универсальные подсказки (когда нет конкретного контекста)
    UNIVERSAL_HINTS = [
        Hint("Расскажи о себе", "Расскажи что-нибудь о себе, Мира", 2),
        Hint("Как твои дела?", "А как у тебя дела, как твоя семья?", 2),
        Hint("Покажи фото", "Покажи своё фото 📷", 3),
    ]

    # Подсказки по контексту ответа Миры
    RESPONSE_TRIGGERS = {
        # Если Мира задала вопрос
        r"\?$": [
            Hint("Да", "Да, именно так", 7),
            Hint("Нет", "Нет, не совсем", 7),
            Hint("Не уверена", "Не уверена...", 6),
        ],
        # Если Мира предложила подумать
        r"подумай|задумайся|попробуй представить": [
            Hint("Думаю об этом", "Да, я думаю об этом...", 5),
            Hint("Сложно", "Мне сложно это представить...", 5),
            Hint("Ты права", "Да, ты права...", 4),
        ],
        # Если Мира рассказала историю
        r"у меня была знакомая|одна моя подруга|помню случай": [
            Hint("У меня похоже", "У меня похожая ситуация...", 5),
            Hint("А что она сделала?", "И что она сделала в итоге?", 4),
            Hint("Это про меня", "Это прямо про меня...", 5),
        ],
        # Если Мира выразила поддержку
        r"я рядом|я здесь|обнимаю|💛": [
            Hint("Спасибо 💛", "Спасибо, что ты рядом 💛", 6),
            Hint("Мне легче", "Мне стало немного легче...", 5),
            Hint("Продолжу рассказ", "Хочу продолжить рассказывать...", 4),
        ],
        # Если Мира просит фото
        r"покажи|скинь фото|фотк|покажешь|хочу посмотреть|интересно увидеть|📸|👀": [
            Hint("📷 Отправлю фото", "Сейчас пришлю фото!", 8),
            Hint("Нет фото", "К сожалению, фото нет...", 5),
            Hint("Потом покажу", "Позже покажу, сейчас неудобно", 4),
        ],
        # Если Мира предлагает музыку
        r"музык|включ|постав|послушать|подбер|предложить|жанр|исполнител|🎧|🎸|🎤|🌙|🎵": [
            Hint("🎵 Включи!", "Да, включи что-нибудь!", 10),
            Hint("Спокойную 🌙", "Хочу что-то спокойное", 9),
            Hint("Энергичную 🎧", "Давай энергичное!", 9),
        ],
        # Если музыка уже играет (Мира спрашивает понравилось ли)
        r"как тебе\?|этот как|вот\!|вот\.{3}|заходит\?|нравится\?|подходящ|поэкспериментир|лучше\?|ещё.*трек|другой трек|лови|держи|ставлю.*трек|\*включаю": [
            Hint("👍 Нравится!", "Да, классный трек!", 12),
            Hint("🔄 Другой", "Давай что-нибудь другое", 11),
            Hint("🛑 Хватит", "Хватит музыки, давай поболтаем", 10),
        ],
    }

    # Подсказки на начало разговора (первые сообщения)
    STARTER_HINTS = [
        Hint("Как дела", "Привет! Как твои дела?", 3),
        Hint("Нужно выговориться", "Мне нужно просто выговориться...", 5),
        Hint("Плохой день", "У меня был тяжёлый день...", 5),
        Hint("Расскажи о себе", "Расскажи немного о себе", 2),
    ]

    def generate(
        self,
        response_text: str,
        tags: List[str],
        mood_data: Optional[Dict[str, Any]] = None,
        message_count: int = 0,
        communication_style: Optional[Dict[str, Any]] = None,
    ) -> List[Hint]:
        """
        Генерирует подсказки на основе контекста.

        Args:
            response_text: Текст ответа Миры
            tags: Теги из анализа разговора
            mood_data: Данные о настроении пользователя
            message_count: Количество сообщений в разговоре
            communication_style: Стиль общения пользователя

        Returns:
            Список подсказок (максимум MAX_HINTS)
        """
        hints: List[Hint] = []

        # 1. Если это начало разговора
        if message_count < 3:
            hints.extend(self.STARTER_HINTS)

        # 2. Анализируем теги тем
        for tag in tags:
            if tag in self.TOPIC_HINTS:
                hints.extend(self.TOPIC_HINTS[tag])

        # 3. Анализируем эмоциональное состояние
        if mood_data:
            # Поддержка как dict, так и объекта MoodEntry
            if hasattr(mood_data, "primary_emotion"):
                emotion = mood_data.primary_emotion
            else:
                emotion = mood_data.get("primary_emotion")
            if emotion in self.EMOTION_HINTS:
                hints.extend(self.EMOTION_HINTS[emotion])

        # 4. Анализируем ответ Миры на триггеры
        response_lower = response_text.lower()

        # Проверяем — музыка уже играет? (приоритетная проверка)
        music_playing_pattern = r"музыка играет|сменить|как тебе\?|этот как|вот\!|вот\.{3}|заходит\?|нравится\?|подходящ|поэкспериментир|лучше\?|ещё.*трек|другой трек|лови|держи|ставлю.*трек|\*включаю|отдыхать под|наслаждайся|расслабляйся"
        music_is_playing = bool(re.search(music_playing_pattern, response_lower))

        for pattern, trigger_hints in self.RESPONSE_TRIGGERS.items():
            # Пропускаем паттерн "предложить музыку" если музыка уже играет
            if music_is_playing and any("Включи" in h.text for h in trigger_hints):
                continue
            if re.search(pattern, response_lower):
                hints.extend(trigger_hints)

        # 5. Если подсказок мало — добавляем универсальные
        if len(hints) < 2:
            hints.extend(self.UNIVERSAL_HINTS)

        # 6. Сортируем по приоритету и убираем дубликаты
        seen_texts = set()
        unique_hints = []
        for hint in sorted(hints, key=lambda h: h.priority, reverse=True):
            if hint.text not in seen_texts:
                seen_texts.add(hint.text)
                unique_hints.append(hint)

        # Возвращаем топ-N
        result = unique_hints[:self.MAX_HINTS]

        # 7. Адаптируем под стиль общения пользователя
        if communication_style:
            result = self._adapt_to_style(result, communication_style)

        logger.debug(
            f"Generated {len(result)} hints from {len(hints)} candidates, "
            f"tags={tags}"
        )

        return result

    def _adapt_to_style(
        self,
        hints: List[Hint],
        style: Dict[str, Any],
    ) -> List[Hint]:
        """
        Адаптирует подсказки под стиль общения пользователя.

        Args:
            hints: Исходные подсказки
            style: Стиль общения пользователя

        Returns:
            Адаптированные подсказки
        """
        adapted = []

        formality = style.get("formality", "informal")
        emoji_pref = style.get("emoji_preference", "few")
        msg_length = style.get("message_length", "medium")

        for hint in hints:
            text = hint.text
            message = hint.message

            # Убираем эмодзи из сообщения если пользователь их не использует
            # НО оставляем в тексте кнопки — они помогают различать варианты
            if emoji_pref == "none":
                message = re.sub(r'[\U0001F300-\U0001F9FF]', '', message).strip()

            # Сокращаем для коротких сообщений
            if msg_length == "short" and len(message) > 30:
                # Обрезаем до первого знака препинания или 30 символов
                match = re.search(r'^[^.!?…]+[.!?…]?', message)
                if match and len(match.group()) < len(message):
                    message = match.group().strip()

            adapted.append(Hint(text=text, message=message, priority=hint.priority))

        return adapted

    def generate_for_crisis(self) -> List[Hint]:
        """Генерирует подсказки для кризисной ситуации."""
        return [
            Hint("Мне плохо", "Мне очень плохо сейчас...", 10),
            Hint("Нужна помощь", "Мне нужна помощь...", 10),
            Hint("Хочу поговорить", "Хочу просто поговорить...", 9),
        ]

    def generate_after_photo(self) -> List[Hint]:
        """Генерирует подсказки после отправки фото."""
        return [
            Hint("Это было...", "Это было вчера/недавно...", 5),
            Hint("Расскажу историю", "Хочу рассказать, что там было...", 6),
            Hint("А у тебя есть?", "А у тебя есть похожие фото?", 4),
        ]


# Глобальный экземпляр
hint_generator = HintGenerator()
