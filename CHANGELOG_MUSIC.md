# Отключение музыкальной функции (14.12.2024)

## Причина

По запросу пользователя музыкальная функция была полностью отключена из-за некорректного поведения.

## Проблемы, которые привели к отключению

1. **Музыка отправлялась слишком рано**
   - При простом упоминании слова "музыка" в ответе Миры
   - Даже когда Мира только предлагала музыку ("хочешь музыку?")
   - Без явного action от Миры (*включаю*, *ставлю*)

2. **Некорректные контекстные подсказки**
   - После отправки музыки показывались кнопки "Включи!", "Спокойную", "Энергичную"
   - Вместо правильных "Нравится!", "Другой", "Хватит"
   - Логика определения контекста работала нестабильно

## Изменения в коде

### 1. bot/handlers/message.py

**Отключён вызов музыки:**

```python
# 8.7. Музыка отключена
# try:
#     await _maybe_send_music(
#         update=update,
#         context=context,
#         mira_response=result["response"],
#         user_message=message_text,
#         mood=primary_mood,
#     )
# except Exception as e:
#     logger.debug(f"Music send error: {e}")
```

**Функция _maybe_send_music остаётся в коде** (строка 781), но не вызывается. Это позволяет легко включить музыку обратно при необходимости.

### 2. ai/hint_generator.py

**Удалены музыкальные паттерны из RESPONSE_TRIGGERS:**

```python
# Музыка отключена
# r"музык|включ|постав|послушать|подбер|предложить|жанр|исполнител|🎧|🎸|🎤|🌙|🎵": [
#     Hint("🎵 Включи!", "Да, включи что-нибудь!", 10),
#     Hint("Спокойную 🌙", "Хочу что-то спокойное", 9),
#     Hint("Энергичную 🎧", "Давай энергичное!", 9),
# ],
# r"наслаждайся|успокаивает|расслабляйся|отдыхай|пусть музыка|под музыку|...": [
#     Hint("👍 Нравится!", "Да, классный трек!", 12),
#     Hint("🔄 Другой", "Давай что-нибудь другое", 11),
#     Hint("🛑 Хватит", "Хватит музыки, давай поболтаем", 10),
# ],
```

**Упрощена логика generate()** — убрана проверка `music_is_playing`:

```python
# До:
music_playing_pattern = r"наслаждайся|успокаивает|..."
music_is_playing = bool(re.search(music_playing_pattern, response_lower))

for pattern, trigger_hints in self.RESPONSE_TRIGGERS.items():
    if music_is_playing and any("Включи" in h.text for h in trigger_hints):
        continue
    if re.search(pattern, response_lower):
        hints.extend(trigger_hints)

# После:
for pattern, trigger_hints in self.RESPONSE_TRIGGERS.items():
    if re.search(pattern, response_lower):
        hints.extend(trigger_hints)
```

### 3. bot/handlers/start.py

**Убраны упоминания "виртуальный":**

```python
# До:
Я — твой виртуальный друг. Не психолог, не терапевт...

# После:
Я — твой друг. Не психолог, не терапевт...
```

## Как включить музыку обратно

Если потребуется вернуть музыку:

1. **Раскомментировать вызов в bot/handlers/message.py** (строки 168-178)
2. **Раскомментировать паттерны в ai/hint_generator.py** (строки 128-138)
3. **Вернуть логику проверки music_is_playing** (если нужна)
4. **Перезапустить бота:** `systemctl restart mirabot`

## Связанные файлы

### Не изменённые (музыка остаётся):

- `services/music_forwarder.py` — сервис пересылки музыки из источника
- `bot/handlers/music.py` — обработчик музыкальных запросов
- `config/music_sources.json` — конфигурация источников музыки

Эти файлы остаются в коде для возможного будущего использования.

## Дата изменения

14.12.2024 (вечер)

## Автор

Claude Code (по запросу пользователя)
