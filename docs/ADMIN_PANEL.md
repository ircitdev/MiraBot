# Админ-панель MIRA BOT

## Доступ к админ-панели

URL: `http://your-server:8081/admin` или через Telegram WebApp

**Требования:**
- Ваш Telegram ID должен быть добавлен в список `ADMIN_IDS` в файле `webapp/api/routes/admin.py`
- Или установите `ADMIN_TELEGRAM_ID` в `config/settings.py`

## Функции админ-панели

### 1. Системная статистика

Отображается в верхней части страницы:

- **Всего пользователей** - количество зарегистрированных пользователей
- **Активны сегодня** - пользователи, которые писали сегодня
- **Активны за неделю** - пользователи, активные за последние 7 дней
- **Всего сообщений** - общее количество сообщений в системе
- **Сообщений сегодня** - сообщения за сегодня
- **Premium пользователей** - количество активных Premium подписок

### 2. Управление пользователями

#### Поиск пользователей

Используйте строку поиска для фильтрации по:
- Имени пользователя
- Username (без @)
- Display name

#### Просмотр таблицы пользователей

Таблица отображает:
- **ID** - Telegram ID пользователя
- **Имя** - Display name или first name
- **Username** - @username
- **Подписка** - badge с типом подписки (Premium/Trial/Free)
- **Сообщений** - общее количество сообщений
- **Последняя активность** - дата и время последнего сообщения

#### Действия с пользователями

**👁️ Просмотр** - показывает детальную информацию:
- ID, имя, username
- Подписка и срок действия
- Статистика сообщений (всего и за неделю)
- Информация о партнёре
- Активные ритуалы
- Статус блокировки

**💬 Переписка** - просмотр истории переписки:
- Открывает модальное окно с последними 200 сообщениями
- Сообщения пользователя справа (фиолетовый фон)
- Сообщения Миры слева (синий фон)
- Отображаются дата, время и теги сообщений
- Без необходимости скачивания файла

**⭐ Выдать Premium** - выдать Premium подписку:
- Запрашивает количество дней (по умолчанию 30)
- Создаёт или обновляет подписку пользователя
- Автоматически обновляет таблицу

**📥 Экспорт истории** - скачать историю переписки:
- Экспортирует все сообщения пользователя в CSV
- Формат: Дата, Роль, Сообщение, Тип, Теги
- Файл: `admin_export_{telegram_id}_history.csv`

### 3. API Endpoints

Все эндпоинты требуют авторизации админа через `X-Telegram-Init-Data` header.

#### GET `/api/admin/users`

Получить список пользователей с фильтрацией.

**Query параметры:**
- `search` (optional) - строка поиска
- `subscription` (optional) - фильтр по типу подписки
- `active_days` (optional) - фильтр по активности
- `limit` (default: 50, max: 500) - количество результатов
- `offset` (default: 0) - смещение для пагинации

**Ответ:**
```json
[
  {
    "id": 1,
    "telegram_id": 123456789,
    "username": "user",
    "first_name": "Имя",
    "display_name": "Отображаемое имя",
    "subscription_plan": "premium",
    "subscription_expires_at": "2025-01-15T00:00:00",
    "total_messages": 150,
    "last_active_at": "2025-12-14T10:30:00",
    "created_at": "2025-11-01T12:00:00"
  }
]
```

#### GET `/api/admin/users/{telegram_id}`

Получить детальную информацию о пользователе.

**Ответ:**
```json
{
  "id": 1,
  "telegram_id": 123456789,
  "username": "user",
  "first_name": "Имя",
  "display_name": "Отображаемое имя",
  "persona": "mira",
  "subscription_plan": "premium",
  "subscription_expires_at": "2025-01-15T00:00:00",
  "total_messages": 150,
  "messages_this_week": 25,
  "last_active_at": "2025-12-14T10:30:00",
  "created_at": "2025-11-01T12:00:00",
  "onboarding_completed": true,
  "is_blocked": false,
  "block_reason": null,
  "partner_name": "Андрей",
  "rituals_enabled": ["morning", "evening"]
}
```

#### POST `/api/admin/users/{telegram_id}/subscription`

Обновить подписку пользователя.

**Body:**
```json
{
  "plan": "premium",
  "days": 30
}
```

**Ответ:**
```json
{
  "status": "ok",
  "user_id": 1,
  "plan": "premium",
  "expires_at": "2025-01-15T00:00:00"
}
```

#### POST `/api/admin/users/{telegram_id}/block`

Заблокировать пользователя.

**Body:**
```json
{
  "reason": "Нарушение правил"
}
```

**Ответ:**
```json
{
  "status": "ok",
  "user_id": 1,
  "blocked": true
}
```

#### POST `/api/admin/users/{telegram_id}/unblock`

Разблокировать пользователя.

**Ответ:**
```json
{
  "status": "ok",
  "user_id": 1,
  "blocked": false
}
```

#### GET `/api/admin/stats/system`

Получить системную статистику.

**Ответ:**
```json
{
  "total_users": 1500,
  "active_users_today": 120,
  "active_users_week": 450,
  "total_messages": 15000,
  "messages_today": 850,
  "premium_users": 50,
  "trial_users": 100,
  "free_users": 1350
}
```

#### GET `/api/admin/users/{telegram_id}/export/history`

Экспортировать историю переписки пользователя в CSV.

**Ответ:** CSV файл с историей сообщений

#### GET `/api/admin/users/{telegram_id}/messages`

Получить сообщения пользователя для просмотра в интерфейсе.

**Query параметры:**
- `limit` (default: 200, max: 1000) - количество последних сообщений

**Ответ:**
```json
[
  {
    "id": 1,
    "role": "user",
    "content": "Привет",
    "created_at": "2025-12-14T10:30:00",
    "message_type": "text",
    "tags": ["greeting"]
  },
  {
    "id": 2,
    "role": "assistant",
    "content": "Привет! Как дела?",
    "created_at": "2025-12-14T10:30:05",
    "message_type": "text",
    "tags": []
  }
]
```

## Настройка админов

### Способ 1: Через config/settings.py

Добавьте в `config/settings.py`:

```python
ADMIN_TELEGRAM_ID = 123456789  # Ваш Telegram ID
```

### Способ 2: Через список в admin.py

Отредактируйте файл `webapp/api/routes/admin.py`:

```python
ADMIN_IDS = [
    123456789,  # Первый админ
    987654321,  # Второй админ
    # Добавьте сюда Telegram ID админов
]
```

## Безопасность

⚠️ **Важно:**
- Доступ к админ-панели имеют только пользователи из списка `ADMIN_IDS`
- Все запросы проверяются через `require_admin` dependency
- Используется Telegram WebApp authentication через `initData`
- При попытке несанкционированного доступа возвращается 403 Forbidden

## Примеры использования

### Выдать Premium пользователю

1. Найдите пользователя в таблице или через поиск
2. Нажмите кнопку **⭐** (Выдать Premium)
3. Введите количество дней (например, 30)
4. Подписка будет создана/обновлена автоматически

### Экспортировать историю

1. Найдите пользователя
2. Нажмите кнопку **📥** (Экспорт)
3. CSV файл скачается автоматически

### Просмотреть статистику пользователя

1. Найдите пользователя
2. Нажмите кнопку **👁️** (Просмотр)
3. Появится alert с детальной информацией

### Просмотреть переписку

1. Найдите пользователя
2. Нажмите кнопку **💬** (Переписка)
3. Откроется модальное окно с историей сообщений
4. Прокрутите вверх/вниз для просмотра
5. Нажмите "Закрыть" или кликните вне окна для выхода

## Технические детали

**Backend:**
- FastAPI роутер: `webapp/api/routes/admin.py`
- Middleware: `require_admin` dependency
- Интеграция с существующими repositories

**Frontend:**
- HTML страница: `webapp/frontend/admin.html`
- Vanilla JavaScript (без дополнительных зависимостей)
- Telegram WebApp SDK для аутентификации

**База данных:**
- Использует существующие модели User, Message, Subscription
- Не требует дополнительных таблиц или миграций
