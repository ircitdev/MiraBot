# Админ-панель Мира - Полное руководство

## Оглавление

1. [Общая информация](#общая-информация)
2. [Доступ к админ-панели](#доступ-к-админ-панели)
3. [Обзор интерфейса](#обзор-интерфейса)
4. [Статистика и метрики](#статистика-и-метрики)
5. [Управление пользователями](#управление-пользователями)
6. [Массовая рассылка](#массовая-рассылка)
7. [Аналитика](#аналитика)
8. [API Endpoints](#api-endpoints)
9. [Безопасность](#безопасность)
10. [Troubleshooting](#troubleshooting)

---

## Общая информация

Админ-панель Мира — это веб-интерфейс для управления ботом, пользователями и аналитикой.

**Технологии:**
- Backend: FastAPI (Python)
- Frontend: Vanilla JS + Material Design 3
- База данных: PostgreSQL
- Графики: Chart.js

**Особенности:**
- Responsive дизайн (работает на всех устройствах)
- Dark/Light тема
- Real-time обновление статистики
- Telegram Web App интеграция
- Клиентская пагинация и фильтрация

---

## Доступ к админ-панели

### Способ 1: Web-версия (с токеном)

1. Получите admin token из переменной окружения `ADMIN_TOKEN`
2. Откройте: `https://your-domain.com/admin?token=YOUR_ADMIN_TOKEN`
3. Токен сохранится в localStorage браузера

### Способ 2: Telegram Web App

1. Отправьте боту команду `/admin` (если вы в списке админов)
2. Бот отправит ссылку с автоматической авторизацией
3. Откроется встроенный WebApp

### Список администраторов

Админы определяются в `webapp/api/routes/admin.py`:

```python
ADMIN_IDS = [
    getattr(settings, 'ADMIN_TELEGRAM_ID', 0),
    65876198,  # Дополнительный админ
]
```

**Как добавить нового админа:**
1. Узнайте Telegram ID пользователя
2. Добавьте ID в массив `ADMIN_IDS`
3. Перезапустите webapp: `systemctl restart mirabot-webapp`

---

## Обзор интерфейса

### Верхняя панель (App Bar)

- **Заголовок:** "Админ-панель Мира"
- **Переключатель темы:** Dark/Light mode

### Главная страница

**Секции (сверху вниз):**

1. **Карточки статистики** (6 блоков)
2. **Retention метрики** (DAU/WAU/MAU)
3. **Графики аналитики** (с табами)
4. **Панель инструментов** (Поиск, Обновить, Рассылка)
5. **Фильтры** (раскрывающаяся панель)
6. **Bulk Actions** (при выборе пользователей)
7. **Таблица пользователей**
8. **Пагинация**

---

## Статистика и метрики

### Основные показатели (верхние карточки)

| Метрика | Описание |
|---------|----------|
| **Всего пользователей** | Общее количество зарегистрированных |
| **Активны сегодня** | Пользователи с last_active_at сегодня |
| **Активны за неделю** | Активность за последние 7 дней |
| **Всего сообщений** | Суммарное количество сообщений |
| **Сообщений сегодня** | Сообщения за сегодня |
| **Premium пользователей** | Пользователи с активной Premium подпиской |

### Retention метрики

**DAU (Daily Active Users):**
- Пользователи, активные за последние 24 часа

**WAU (Weekly Active Users):**
- Пользователи, активные за последние 7 дней

**MAU (Monthly Active Users):**
- Пользователи, активные за последние 30 дней

**DAU/WAU (Stickiness):**
- Процент дневных пользователей от недельных
- Формула: `(DAU / WAU) * 100`
- Показывает "прилипчивость" продукта

**WAU/MAU:**
- Процент недельных от месячных
- Формула: `(WAU / MAU) * 100`

**Интерпретация stickiness:**
- < 10% — низкая вовлеченность
- 10-20% — средняя
- 20-30% — хорошая
- \> 30% — отличная (как у соцсетей)

---

## Управление пользователями

### Поиск пользователей

**Поле поиска:**
- Поиск по имени (first_name)
- Поиск по display_name
- Поиск по username

**Клавиши:**
- `Enter` — выполнить поиск
- `/` — быстрый фокус на поле поиска
- `r` — обновить список

### Фильтрация

**Фильтры (раскрывающаяся панель):**

1. **По подписке:**
   - Premium
   - Trial
   - Free

2. **По активности:**
   - Сегодня
   - За неделю
   - За месяц
   - Неактивные >30 дней

3. **По онбордингу:**
   - Завершён
   - Не завершён

4. **По дате регистрации:**
   - Сегодня
   - За неделю
   - За месяц

**Кнопка "Сбросить"** очищает все фильтры.

### Сортировка

**Кликабельные колонки:**
- ID (telegram_id)
- Имя (name)
- Подписка (subscription_plan)
- Сообщений (total_messages)
- Последняя активность (last_active_at)

**Как работает:**
- Первый клик — сортировка по возрастанию (↑)
- Второй клик — по убыванию (↓)
- Состояние сохраняется в localStorage

### Пагинация

**Настройки:**
- 25 / 50 / 100 пользователей на страницу
- Кнопки "Назад" / "Вперёд"
- Счётчик: "Показано 1-50 из 234 пользователей"

### Действия с пользователем

**Иконки в строке:**

1. **👁️ Просмотр** (`visibility`)
   - Открывает модальное окно с информацией
   - Показывает: ID, имя, подписку, статистику, партнёра, ритуалы, блокировку

2. **💬 Переписка** (`chat`)
   - Открывает историю сообщений
   - Показывает все сообщения (до 200 последних)
   - С тегами и датами

3. **⭐ Выдать Premium** (`star`)
   - Запрашивает количество дней
   - Подтверждение перед выдачей
   - Создаёт или обновляет подписку

4. **⬇️ Экспорт истории** (`download`)
   - Скачивает CSV файл с историей переписки
   - Формат: Дата, Роль, Сообщение, Тип, Теги

### Bulk Actions (массовые операции)

**Когда появляется:**
- При выборе хотя бы одного пользователя (checkbox)

**Панель показывает:**
- Количество выбранных пользователей
- Кнопки массовых действий

**Доступные действия:**

1. **⭐ Выдать Premium**
   - Запрашивает количество дней
   - Применяется ко всем выбранным
   - Показывает прогресс: "✅ Premium выдан N пользователям"

2. **📥 Экспортировать**
   - Собирает данные всех выбранных пользователей
   - Создаёт CSV файл
   - Формат: `mira_bulk_export_{count}_users_{timestamp}.csv`

3. **❌ Снять выделение**
   - Очищает все чекбоксы

### Блокировка пользователя

**Endpoint:** `POST /api/admin/users/{telegram_id}/block`

**Параметры:**
- `reason` (string) — причина блокировки

**Эффект:**
- `user.is_blocked = True`
- `user.block_reason = reason`
- Пользователь не сможет писать боту

**Разблокировка:**

**Endpoint:** `POST /api/admin/users/{telegram_id}/unblock`

---

## Массовая рассылка

### Описание

Функционал для отправки сообщений группам пользователей через бота.

### Как открыть

**Кнопка "Рассылка"** (оранжевая, с иконкой 📣) в toolbar.

### Параметры рассылки

**1. Целевая группа (обязательно):**

| Группа | Описание | Запрос |
|--------|----------|--------|
| Все пользователи | Все незаблокированные | `is_blocked = False` |
| Premium | Активная Premium подписка | `plan='premium' AND expires_at > now()` |
| Trial | Активная Trial подписка | `plan='trial' AND expires_at > now()` |
| Free | Без активной подписки | Нет подписки или истекла |
| Активны сегодня | last_active_at сегодня | `last_active_at >= today_start` |
| Активны за неделю | За последние 7 дней | `last_active_at >= week_ago` |
| Неактивные | Не активны >30 дней | `last_active_at < month_ago OR NULL` |

**2. Сообщение (обязательно):**

- Минимум 5 символов
- Поддержка Markdown:
  - `**жирный**`
  - `*курсив*`
  - `[ссылка](url)`
  - `` `код` ``
- Отправляется с `parse_mode="Markdown"`

**3. Задержка между сообщениями (опционально):**

- Диапазон: 0-10 секунд
- По умолчанию: 1 секунда
- Рекомендуется: 1-2 секунды

**Зачем нужна задержка:**
- Telegram API имеет rate limits
- Без задержки может забанить бота
- 1 сек = ~3600 сообщений/час (безопасно)

### Процесс отправки

**1. Подтверждение:**
- Показывается целевая группа
- Превью сообщения (первые 100 символов)

**2. Запуск:**
- API возвращает:
  - `task_id` (UUID)
  - `target_users_count` (количество получателей)
  - `status: "started"`

**3. Фоновая отправка:**
- Рассылка выполняется асинхронно через `asyncio.create_task`
- Не блокирует UI
- Логируется в файл: `/var/log/mirabot.log`

**4. Логи:**

```
INFO: Starting broadcast {task_id} to {count} users
DEBUG: Broadcast message sent to {telegram_id}
WARNING: Failed to send broadcast to {telegram_id}: {error}
INFO: Broadcast {task_id} completed: {success} success, {fail} failed
```

### Безопасность

**Защиты:**
- ✅ Только для администраторов (`require_admin`)
- ✅ Исключаются заблокированные пользователи
- ✅ Валидация длины сообщения (≥5 символов)
- ✅ Двойное подтверждение в UI
- ✅ Предупреждение о необратимости

**Что НЕ МОЖЕТ делать:**
- ❌ Отменить рассылку после запуска
- ❌ Увидеть прогресс в реальном времени (только в логах)
- ❌ Отправить медиа (только текст)

### Примеры использования

**1. Объявление о новых функциях:**
```
Целевая группа: Все пользователи
Сообщение:
🎉 **Новые возможности!**

Теперь в боте доступны:
• SMART цели с отслеживанием
• Напоминания о планах
• Улучшенная аналитика настроения

Попробуй команду /goals
```

**2. Призыв к активности неактивных:**
```
Целевая группа: Неактивные (>30 дней)
Сообщение:
👋 Привет! Давно не общались...

Мира скучала! Как дела? Что нового?

Напиши мне, и я помогу тебе разобраться с мыслями или просто поболтаю 💜
```

**3. Специальное предложение для Free:**
```
Целевая группа: Free пользователи
Сообщение:
🎁 **Специально для тебя!**

Попробуй Premium бесплатно на 7 дней:
• Безлимитные сообщения
• Продвинутая аналитика
• Голосовые медитации

Команда: /trial
```

---

## Аналитика

### Вкладки аналитики

**5 секций с табами:**

1. **Активность** (Activity)
2. **Подписки** (Subscriptions)
3. **Настроение** (Mood)
4. **Эмоции** (Emotions)
5. **Топ-теги** (Tags)

### 1. Активность

**График:** Line chart (линейный)

**Данные:**
- Активные пользователи (по дням)
- Количество сообщений (по дням)

**Периоды:**
- 7 дней
- 30 дней

**Endpoint:** `GET /api/admin/analytics/activity?days={7|30}`

**Response:**
```json
{
  "labels": ["01.12", "02.12", ...],
  "active_users": [45, 52, 48, ...],
  "messages": [234, 298, 276, ...]
}
```

### 2. Подписки

**График:** Doughnut chart (пончик)

**Данные:**
- Premium (количество)
- Trial (количество)
- Free (количество)

**Расчёт Free:**
- `total_users - premium_count - trial_count`

**Endpoint:** `GET /api/admin/analytics/subscriptions`

**Response:**
```json
{
  "labels": ["Premium", "Trial", "Free"],
  "values": [12, 8, 234]
}
```

### 3. Настроение

**График:** Line chart с двумя осями

**Данные:**
- Средний балл настроения (0-10)
- Количество записей

**Периоды:**
- 7 дней
- 30 дней
- 90 дней

**Endpoint:** `GET /api/admin/analytics/mood?days={7|30|90}`

**Response:**
```json
{
  "labels": ["01.12", "02.12", ...],
  "average_mood": [7.2, 6.8, 7.5, ...],
  "entry_counts": [23, 28, 19, ...]
}
```

**Интерпретация баллов:**
- 0-3: Очень плохое настроение
- 4-5: Плохое
- 6-7: Нейтральное
- 8-9: Хорошее
- 10: Отличное

### 4. Эмоции

**График:** Bar chart (столбчатая диаграмма)

**Данные:**
- Распределение primary_emotion

**Эмоции (на русском):**
- Радость (joy)
- Грусть (sadness)
- Тревога (anxiety)
- Гнев (anger)
- Страх (fear)
- Спокойствие (calm)
- Раздражение (frustration)
- Надежда (hope)
- Одиночество (loneliness)
- Перегружена (overwhelmed)
- Нейтрально (neutral)

**Периоды:**
- 7 дней
- 30 дней
- 90 дней

**Endpoint:** `GET /api/admin/analytics/emotions?days={7|30|90}`

### 5. Топ-теги

**График:** Horizontal bar chart (горизонтальные столбцы)

**Данные:**
- Топ-5 самых частых тегов сообщений

**Периоды:**
- 7 дней
- 30 дней
- 90 дней

**Endpoint:** `GET /api/admin/analytics/tags?days={7|30|90}`

**Как считается:**
- Берутся все сообщения с `tags IS NOT NULL`
- Разворачивается массив тегов
- Подсчитывается частота каждого тега
- Сортируется по убыванию
- Берётся топ-5

---

## API Endpoints

### Базовый URL

```
/api/admin
```

### Авторизация

**Два способа:**

1. **Admin Token (Bearer):**
```http
Authorization: Bearer YOUR_ADMIN_TOKEN
```

2. **Telegram Init Data:**
```http
X-Telegram-Init-Data: query_id=...&user=...
```

### Список эндпоинтов

#### Пользователи

**GET /users**
- Список пользователей с фильтрацией
- Params: `search`, `subscription`, `active_days`, `limit`, `offset`
- Response: `List[UserListItem]`

**GET /users/{telegram_id}**
- Детальная информация о пользователе
- Response: `UserDetailResponse`

**POST /users/{telegram_id}/subscription**
- Обновить подписку
- Body: `{"plan": "premium", "days": 30}`
- Response: `{"status": "ok", "user_id": ..., "plan": ..., "expires_at": ...}`

**POST /users/{telegram_id}/block**
- Заблокировать пользователя
- Body: `{"reason": "spam"}`
- Response: `{"status": "ok", "user_id": ..., "blocked": true}`

**POST /users/{telegram_id}/unblock**
- Разблокировать
- Response: `{"status": "ok", "user_id": ..., "blocked": false}`

**GET /users/{telegram_id}/messages**
- История сообщений
- Params: `limit` (default: 200, max: 1000)
- Response: `List[MessageItem]`

**GET /users/{telegram_id}/export/history**
- Экспорт истории в CSV
- Response: CSV file

#### Статистика

**GET /stats/system**
- Системная статистика
- Response: `SystemStatsResponse`

**GET /analytics/retention**
- Retention метрики (DAU/WAU/MAU)
- Response: `RetentionMetricsResponse`

**GET /analytics/activity**
- График активности
- Params: `days` (7|30|90)
- Response: `ActivityDataResponse`

**GET /analytics/subscriptions**
- Распределение подписок
- Response: `SubscriptionsDataResponse`

**GET /analytics/mood**
- График настроения
- Params: `days` (7|30|90)
- Response: `MoodOverTimeResponse`

**GET /analytics/emotions**
- Распределение эмоций
- Params: `days` (7|30|90)
- Response: `EmotionDistributionResponse`

**GET /analytics/tags**
- Топ-5 тегов
- Params: `days` (7|30|90)
- Response: `TopTagsResponse`

#### Рассылка

**POST /broadcast**
- Создать массовую рассылку
- Body: `BroadcastRequest`
- Response: `BroadcastResponse`

**Request:**
```json
{
  "message": "Текст сообщения",
  "target_group": "all",
  "delay_seconds": 1
}
```

**Response:**
```json
{
  "status": "started",
  "task_id": "uuid",
  "target_users_count": 234,
  "message": "Рассылка запущена для 234 пользователей"
}
```

---

## Безопасность

### Проверка прав

**Функция `require_admin`:**
```python
async def require_admin(current_user: dict = Depends(get_current_user)):
    if not is_admin(current_user):
        raise HTTPException(status_code=403, detail="Access denied. Admin only.")
    return current_user
```

**Применяется ко ВСЕМ эндпоинтам:**
- Если токен невалиден → 401 Unauthorized
- Если user_id не в ADMIN_IDS → 403 Forbidden

### Защита от злоупотреблений

**Рассылка:**
- Только админы
- Исключаются заблокированные пользователи
- Rate limiting через delay_seconds
- Логирование всех действий

**Блокировка пользователей:**
- Только админы
- Требуется указать причину
- Логируется в БД

**Экспорт данных:**
- Только админы
- Ограничение по количеству сообщений (1000 max)

### Логирование

**Все admin actions логируются:**
```python
logger.info(f"Admin {admin_id} granted premium to user {user_id} for {days} days")
logger.warning(f"Admin {admin_id} blocked user {user_id}: {reason}")
logger.info(f"Admin {admin_id} started broadcast {task_id} to {count} users")
```

**Расположение логов:**
- WebApp: `/var/log/mira_webapp.log`
- Bot: `/var/log/mirabot.log`

---

## Troubleshooting

### Проблема: "Unauthorized" при входе

**Причины:**
1. Неверный токен
2. Токен не передан

**Решение:**
1. Проверьте URL: `?token=...`
2. Проверьте localStorage: `localStorage.getItem('admin_token')`
3. Получите новый токен из env: `echo $ADMIN_TOKEN`

### Проблема: "Access denied. Admin only"

**Причина:**
- Ваш telegram_id не в списке ADMIN_IDS

**Решение:**
1. Узнайте свой telegram_id: отправьте `/start` боту
2. Добавьте в `webapp/api/routes/admin.py`:
```python
ADMIN_IDS = [
    65876198,
    YOUR_ID_HERE,  # <- добавьте сюда
]
```
3. Перезапустите webapp

### Проблема: Рассылка не отправляется

**Возможные причины:**

1. **Bot не запущен:**
```bash
systemctl status mirabot
```

2. **Недостаточно памяти:**
```bash
free -h
```

3. **Rate limits от Telegram:**
- Уменьшите delay_seconds до 2-3 сек
- Разбейте рассылку на части

4. **Ошибки в логах:**
```bash
tail -f /var/log/mirabot.log | grep broadcast
```

### Проблема: Графики не загружаются

**Причины:**
1. Нет данных за выбранный период
2. Ошибка на сервере

**Решение:**
1. Откройте DevTools (F12) → Console
2. Проверьте Network → найдите запрос к `/api/admin/analytics/...`
3. Проверьте статус ответа:
   - 200 OK → всё хорошо
   - 401/403 → проблема с авторизацией
   - 500 → ошибка на сервере (смотрите логи)

### Проблема: Таблица пользователей пустая

**Причины:**
1. Нет пользователей в БД
2. Применены фильтры
3. Ошибка загрузки

**Решение:**
1. Проверьте фильтры → нажмите "Сбросить"
2. Проверьте поиск → очистите поле
3. Нажмите "Обновить"
4. Проверьте консоль браузера на ошибки

### Проблема: WebApp не запускается

**Диагностика:**
```bash
# Проверить статус
systemctl status mirabot-webapp

# Проверить порт
netstat -tulpn | grep 8081

# Проверить логи
tail -50 /var/log/mira_webapp.log
```

**Частые ошибки:**

1. **"Address already in use":**
```bash
# Найти процесс
lsof -i :8081

# Убить
kill -9 <PID>

# Перезапустить
systemctl restart mirabot-webapp
```

2. **"Module not found":**
```bash
# Активировать venv
cd /root/mira_bot
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

---

## Keyboard Shortcuts

| Клавиша | Действие |
|---------|----------|
| `/` | Фокус на поле поиска |
| `r` | Обновить список пользователей |
| `Escape` | Закрыть открытые диалоги |
| `Enter` в поиске | Выполнить поиск |

---

## Полезные команды

### Мониторинг

```bash
# Статус всех сервисов
systemctl status mirabot
systemctl status mirabot-webapp

# Логи в реальном времени
tail -f /var/log/mirabot.log
tail -f /var/log/mira_webapp.log

# Использование ресурсов
htop
```

### Перезапуск сервисов

```bash
# Перезапустить бота
systemctl restart mirabot

# Перезапустить webapp
systemctl restart mirabot-webapp

# Оба сразу
systemctl restart mirabot mirabot-webapp
```

### База данных

```bash
# Подключиться к PostgreSQL
psql -U postgres -d mira_bot

# Проверить количество пользователей
SELECT COUNT(*) FROM users;

# Проверить активных сегодня
SELECT COUNT(*) FROM users WHERE last_active_at >= CURRENT_DATE;

# Топ-10 активных пользователей
SELECT telegram_id, first_name, COUNT(*) as msg_count
FROM messages m
JOIN users u ON m.user_id = u.id
GROUP BY telegram_id, first_name
ORDER BY msg_count DESC
LIMIT 10;
```

---

## Roadmap

**Планируемые улучшения:**

1. **Расширенная аналитика:**
   - Когорты пользователей
   - Retention curves
   - Funnel analysis

2. **Больше возможностей рассылки:**
   - Отправка медиа (фото, видео, стикеры)
   - Планирование на определённое время
   - A/B тестирование сообщений
   - Отмена рассылки
   - Прогресс бар в UI

3. **Управление контентом:**
   - Загрузка медитаций
   - Редактирование дыхательных практик
   - Управление стикер-паками

4. **CRM функции:**
   - Заметки к пользователям
   - Теги и сегменты
   - Автоматические действия (триггеры)

5. **Улучшения UI:**
   - Экспорт графиков в PNG
   - Настройка дашборда (drag-n-drop виджеты)
   - Темная тема с кастомизацией

---

## Контакты

**Документация:**
- Основная: `docs/ADMIN_PANEL.md`
- Быстрый старт: `docs/ADMIN_SETUP.md`
- TODO: `docs/TODO_ADMIN_IMPROVEMENTS.md`

**Код:**
- Backend: `webapp/api/routes/admin.py`
- Frontend: `webapp/frontend/admin.html`

**Логи:**
- WebApp: `/var/log/mira_webapp.log`
- Bot: `/var/log/mirabot.log`

**GitHub:**
- Repository: https://github.com/ircitdev/MiraBot

---

**Последнее обновление:** 15.12.2025
**Версия:** 1.0.0
**Автор:** Claude Code + ircitdev team
