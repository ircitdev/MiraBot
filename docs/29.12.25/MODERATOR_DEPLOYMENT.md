# Развертывание системы модераторского доступа

**Дата:** 29.12.2025
**Версия:** v2.2.0
**Статус:** Готово к развертыванию

---

## 📋 Обзор

Система модераторского доступа добавляет двухуровневую систему прав:
- **Администратор** — полный доступ ко всем операциям
- **Модератор** — доступ ко всем функциям, кроме удаления

Все действия администраторов и модераторов логируются с возможностью просмотра в интерфейсе.

---

## 🚀 Инструкция по развертыванию

### Шаг 1: Получить изменения с GitHub

```bash
cd /path/to/mira_bot
git pull origin main
```

**Ожидаемый результат:**
```
remote: Counting objects: 15, done.
...
   d6267e0..b744d2b  main -> main
```

### Шаг 2: Применить миграции базы данных

```bash
# Проверить текущую версию БД
alembic current

# Применить миграции
alembic upgrade head
```

**Ожидаемый вывод:**
```
INFO  [alembic.runtime.migration] Running upgrade 5da41d0baade -> 20251229_add_admin_users_and_logs, add admin users and logs
```

**Проверка:**
```bash
# Проверить что таблицы созданы
sqlite3 data/mira.db "SELECT name FROM sqlite_master WHERE type='table' AND name IN ('admin_users', 'admin_logs');"
```

Должно вывести:
```
admin_users
admin_logs
```

### Шаг 3: Назначить первого модератора

#### Вариант А: Упрощенный скрипт (рекомендуется)

```bash
python scripts/init_moderator_simple.py \
  --telegram-id 1392513515 \
  --role moderator \
  --first-name "Лиза"
```

#### Вариант Б: Полный скрипт (требует наличия пользователя в БД)

```bash
python scripts/init_moderator.py \
  --telegram-id 1392513515 \
  --role moderator
```

**Ожидаемый вывод:**
```
============================================================
  Назначение moderator
============================================================

✅ Пользователь 1392513515 назначен как 'moderator'
   ID записи: 1
   Имя: Лиза
   Username: @—
   Роль: moderator
   Активен: True

============================================================
  ✅ Операция завершена успешно
============================================================
```

### Шаг 4: Перезапустить приложение

```bash
# Если используется systemd
sudo systemctl restart mira-bot
sudo systemctl restart mira-webapp

# Или Docker
docker-compose restart
```

### Шаг 5: Проверить работоспособность

1. **Проверить API:**
```bash
# Получить токен доступа для Telegram ID 1392513515
# (из админ-панели или через initData)

curl -X GET "http://localhost:8000/api/moderators/me" \
  -H "Authorization: tma YOUR_TELEGRAM_INIT_DATA"
```

Ожидается ответ:
```json
{
  "id": 1,
  "telegram_id": 1392513515,
  "username": null,
  "first_name": "Лиза",
  "last_name": null,
  "role": "moderator",
  "avatar_url": null,
  "accent_color": "#1976d2",
  "is_active": true,
  "created_at": "2025-12-29T...",
  "last_login_at": null
}
```

2. **Проверить админ-панель:**
   - Открыть `https://mira.uspeshnyy.ru/admin`
   - В шапке должен появиться профиль модератора
   - Доступны разделы: Профиль модератора, Мои действия
   - В Конфиг → Лог операций видны все логи

---

## 📊 Структура таблиц

### admin_users

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | INTEGER | PK |
| telegram_id | BIGINT | Telegram ID (unique, indexed) |
| username | VARCHAR(255) | Username без @ |
| first_name | VARCHAR(255) | Имя |
| last_name | VARCHAR(255) | Фамилия |
| role | VARCHAR(50) | 'admin' или 'moderator' (indexed) |
| avatar_url | VARCHAR(500) | URL аватара |
| accent_color | VARCHAR(7) | HEX цвет (#RRGGBB) |
| created_at | DATETIME | Дата создания |
| created_by_id | INTEGER | FK → admin_users.id |
| is_active | BOOLEAN | Активен ли |
| last_login_at | DATETIME | Последний вход |

**Индексы:**
- `ix_admin_users_telegram_id` (telegram_id)
- `ix_admin_users_role` (role)

### admin_logs

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | INTEGER | PK |
| admin_user_id | INTEGER | FK → admin_users.id (indexed) |
| action | VARCHAR(100) | Тип действия (indexed) |
| resource_type | VARCHAR(50) | Тип ресурса (indexed) |
| resource_id | INTEGER | ID ресурса |
| details | JSON | Детали операции |
| ip_address | VARCHAR(45) | IP адрес |
| user_agent | VARCHAR(500) | User-Agent |
| success | BOOLEAN | Успешность |
| error_message | TEXT | Сообщение об ошибке |
| created_at | DATETIME | Время действия (indexed) |

**Индексы:**
- `ix_admin_logs_admin_user_id` (admin_user_id)
- `ix_admin_logs_action` (action)
- `ix_admin_logs_created_at` (created_at)
- `ix_admin_logs_resource_type` (resource_type)

---

## 🔐 Разграничение прав

### Модератор может:
- ✅ Просматривать пользователей
- ✅ Блокировать/разблокировать пользователей
- ✅ Создавать подписки
- ✅ Редактировать подписки
- ✅ Отправлять рассылки
- ✅ Просматривать статистику
- ✅ Просматривать все логи
- ✅ Просматривать свой профиль
- ✅ Менять акцентный цвет

### Модератор НЕ может:
- ❌ Удалять пользователей
- ❌ Удалять подписки
- ❌ Назначать других модераторов
- ❌ Деактивировать модераторов

### Администратор может:
- ✅ Всё что модератор
- ✅ Удалять пользователей
- ✅ Удалять подписки
- ✅ Назначать модераторов
- ✅ Деактивировать модераторов
- ✅ Изменять роли

---

## 📝 Примеры использования

### Назначить дополнительного модератора

```bash
python scripts/init_moderator_simple.py \
  --telegram-id 987654321 \
  --role moderator \
  --first-name "Имя" \
  --last-name "Фамилия" \
  --username "username"
```

### Повысить модератора до администратора

```bash
python scripts/init_moderator_simple.py \
  --telegram-id 1392513515 \
  --role admin
```

### Просмотреть всех модераторов

```bash
sqlite3 data/mira.db "SELECT id, telegram_id, first_name, role, is_active FROM admin_users;"
```

### Просмотреть последние логи

```bash
sqlite3 data/mira.db "SELECT datetime(created_at, 'localtime') as time, action, resource_type, success FROM admin_logs ORDER BY created_at DESC LIMIT 10;"
```

---

## 🐛 Troubleshooting

### Проблема: "no such column: admin_users.telegram_id"

**Причина:** Миграции не применены

**Решение:**
```bash
alembic upgrade head
```

### Проблема: "Table 'admin_users' already exists"

**Причина:** Таблица создана, но миграция не отмечена как выполненная

**Решение:**
```bash
alembic stamp head
```

### Проблема: Модератор не может войти в админ-панель

**Проверки:**
1. Проверить что модератор активен:
```bash
sqlite3 data/mira.db "SELECT * FROM admin_users WHERE telegram_id=1392513515;"
```

2. Проверить is_active = 1

3. Проверить что используется правильный initData от Telegram

### Проблема: Логи не записываются

**Проверки:**
1. Проверить что декораторы применены к эндпоинтам
2. Проверить что передается `request: Request` в функцию
3. Проверить логи приложения на ошибки

---

## 📚 API Endpoints

### Moderators

- `GET /api/moderators/me` - Текущий модератор
- `GET /api/moderators/` - Список всех (только admin)
- `GET /api/moderators/{id}` - Конкретный модератор
- `POST /api/moderators/` - Создать (только admin)
- `PATCH /api/moderators/{id}` - Обновить (только admin)
- `POST /api/moderators/{id}/activate` - Активировать (только admin)
- `POST /api/moderators/{id}/deactivate` - Деактивировать (только admin)
- `PUT /api/moderators/me/accent-color` - Обновить цвет

### Admin Logs

- `GET /api/admin-logs/` - Все логи с фильтрацией
- `GET /api/admin-logs/me` - Логи текущего модератора
- `GET /api/admin-logs/resource/{type}/{id}` - Логи по ресурсу
- `GET /api/admin-logs/failures` - Ошибки за N часов (только admin)
- `GET /api/admin-logs/{id}` - Конкретный лог

---

## ✅ Чеклист финальной приемки

### База данных
- [ ] Таблицы `admin_users` и `admin_logs` созданы
- [ ] Миграция применена: `alembic current` показывает последнюю версию
- [ ] Индексы настроены корректно

### Backend
- [ ] Все DELETE операции защищены `require_admin_role`
- [ ] Остальные операции используют `get_current_admin`
- [ ] Декораторы логирования применены
- [ ] API endpoints отвечают корректно

### Frontend
- [ ] Шапка админки отображает профиль модератора
- [ ] Выпадающее меню работает
- [ ] Страница профиля работает
- [ ] Страница "Мои действия" работает
- [ ] Вкладка "Лог операций" в Конфиг работает
- [ ] Выбор цвета работает и применяется

### Тестирование
- [ ] Модератор может выполнять разрешённые операции
- [ ] Модератор получает 403 при попытке удаления
- [ ] Администратор может выполнять все операции
- [ ] Все действия логируются
- [ ] Логи отображаются корректно в UI

---

## 🎉 Готово!

После выполнения всех шагов система модераторского доступа полностью функциональна.

**Первый вход модератора:**
1. Лиза открывает админ-панель: `https://mira.uspeshnyy.ru/admin`
2. Авторизуется через Telegram
3. Видит свой профиль в шапке
4. Может управлять пользователями, подписками, рассылками
5. Может просматривать свои действия и все логи

**Контакты для поддержки:**
- GitHub Issues: https://github.com/ircitdev/MiraBot/issues
- Документация: `docs/29.12.25/`
