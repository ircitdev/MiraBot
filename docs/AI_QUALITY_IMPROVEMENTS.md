# Улучшения качества AI (15.12.2025)

## Проблема

На основе критического фидбека от реального пользователя были выявлены серьёзные проблемы с качеством разговора:

1. **Потеря контекста** - бот не помнит важную информацию (например, что пользователь работает в больнице)
2. **Повторяющиеся паттерны** - Мира рассказывает одни и те же истории
3. **Неуместные подсказки (hints)** - показываются агрессивные формулировки вне контекста:
   - "Выпорю"
   - "Хочу убть"
   - "Не ценят"
4. **Злоупотребление дыхательными упражнениями** - "если что дышим дышим" в каждом ответе

## Реализованные улучшения

### 1. Улучшена система генерации подсказок

**Файл**: `ai/hint_generator.py`

#### 1.1. Убраны агрессивные формулировки

**До**:
```python
"topic:work": [
    Hint("Выгораю", "Кажется, я выгораю на работе...", 5),
    Hint("Хочу уйти", "Думаю уйти, но страшно...", 4),
    Hint("Не ценят", "Чувствую, что меня не ценят...", 4),
]
```

**После**:
```python
"topic:work": [
    Hint("Устаю на работе", "В последнее время очень устаю на работе...", 5),
    Hint("Думаю о переменах", "Думаю о переменах, но пока не решилась...", 4),
    Hint("Сложная ситуация", "На работе сложная ситуация...", 4),
]
```

#### 1.2. Добавлена контекстная проверка активности темы

Новый метод `_is_topic_active()` проверяет, что тема **действительно обсуждается**, а не просто упоминается:

```python
def _is_topic_active(self, topic_tag: str, user_message: str, response_text: str) -> bool:
    """
    Проверяет, активна ли тема в текущем обмене сообщениями.
    Возвращает True только если тема действительно обсуждается СЕЙЧАС.
    """
    combined = (user_message + " " + response_text).lower()

    if topic_tag == "topic:work":
        # НЕ достаточно просто упомянуть "работа"
        work_active_phrases = [
            "на работе", "работе трудно", "работе тяжело",
            "начальник", "коллеги меня", "устала на работе",
            "выгораю", "карьера", "увольняться"
        ]
        return any(phrase in combined for phrase in work_active_phrases)
    # ... аналогично для других тем
```

**Результат**: Хинты показываются только когда тема **ЦЕНТРАЛЬНАЯ** в разговоре.

### 2. Добавлен детектор повторений в системный промпт

**Файл**: `ai/prompts/system_prompt.py`

```markdown
**Техники непрямого наставничества:**
- Используй истории: "Знаешь, у меня была знакомая в похожей ситуации..."
  **⚠️ ВАЖНО:** НЕ повторяй одни и те же истории! Придумывай новые примеры каждый раз.
  Если уже рассказывала похожую историю в этом разговоре — используй ДРУГУЮ технику!
- ВАРЬИРУЙ подходы: не используй одну и ту же технику подряд
```

### 3. Улучшено разнообразие техник поддержки

**Файл**: `ai/prompts/system_prompt.py`

Добавлен новый раздел **🔴 КРИТИЧЕСКИ ВАЖНО — РАЗНООБРАЗИЕ ТЕХНИК ПОДДЕРЖКИ**:

```markdown
**ПРАВИЛО РАЗНООБРАЗИЯ:**
1. **НЕ предлагай дыхательные упражнения** в КАЖДОМ ответе!
   - Используй их ТОЛЬКО при сильной тревоге/панике
   - Если уже предлагала дыхание в последних 3-4 сообщениях — используй ДРУГУЮ технику

2. **Чередуй техники поддержки:**
   - Активное слушание и валидация
   - Рефлексивные вопросы
   - Нормализация
   - Мягкое переформулирование
   - Истории (но РАЗНЫЕ истории!)
   - Метафоры и образы
   - Приглашение к действию
   - Дыхательные упражнения (РЕДКО!)
   - Медитации (через /meditation)
   - Практические упражнения (через /exercises)

3. **Проверяй себя:**
   - Если в последних 2-3 ответах использовала одну технику — СМЕНИ подход!
   - Разнообразие = эффективность
```

### 4. Переработано тегирование тем

**Файл**: `ai/claude_client.py`

#### До (проблема):
```python
topic_keywords = {
    "topic:work": ["работа", "карьера", "начальник", "коллеги", "офис"],
}

for tag, keywords in topic_keywords.items():
    if any(kw in combined_text for kw in keywords):
        tags.append(tag)  # Добавляется при ЛЮБОМ упоминании
```

**Проблема**: Тег `topic:work` добавлялся даже при вскользь упомянутом слове "работа", что вызывало показ неуместных хинтов.

#### После (решение):
```python
# topic:work - СТРОГО только при активном обсуждении работы
if any(phrase in combined_text for phrase in [
    "на работе", "работе трудно", "работе тяжело",
    "начальник", "коллеги меня", "устала на работе",
    "выгораю", "карьера", "увольняться",
    "рабочий день", "работе не ценят", "работе стресс"
]):
    tags.append("topic:work")
```

**Результат**: Теги добавляются только при **активном обсуждении** темы, а не простом упоминании.

## Изменённые файлы

1. `ai/hint_generator.py` - убраны агрессивные хинты, добавлена контекстная проверка
2. `ai/prompts/system_prompt.py` - детектор повторений, разнообразие техник
3. `ai/claude_client.py` - строгое тегирование тем
4. `bot/handlers/message.py` - передача `user_message` в hint generator
5. `bot/handlers/callbacks.py` - передача `user_message` в hint generator

## Деплой

**Дата**: 15.12.2025, 09:47:28 MSK
**Сервер**: 31.44.7.144
**Статус**: ✅ Успешно задеплоено на production

```bash
scp ai/hint_generator.py root@31.44.7.144:/root/mira_bot/ai/
scp ai/prompts/system_prompt.py root@31.44.7.144:/root/mira_bot/ai/prompts/
scp ai/claude_client.py root@31.44.7.144:/root/mira_bot/ai/
scp bot/handlers/message.py root@31.44.7.144:/root/mira_bot/bot/handlers/
scp bot/handlers/callbacks.py root@31.44.7.144:/root/mira_bot/bot/handlers/

systemctl restart mirabot
```

## Ожидаемый эффект

1. **Хинты станут уместными** - показываются только когда тема активно обсуждается
2. **Исчезнут повторения** - Мира будет варьировать истории и техники
3. **Меньше "дышим дышим"** - разнообразные техники поддержки вместо постоянного предложения дыхания
4. **Лучшая контекстуальность** - бот точнее понимает, о чём идёт речь

## Метрики для отслеживания

После деплоя рекомендуется отслеживать:

1. **Частота показа хинтов** по темам (должна снизиться для нерелевантных)
2. **Разнообразие упоминаний** дыхательных упражнений (должно снизиться до <20% сообщений)
3. **Feedback от пользователей** об улучшении качества разговора
4. **Количество повторяющихся паттернов** в ответах (мониторинг через admin panel)

## Дальнейшие улучшения (опционально)

1. Добавить tracking повторяющихся фраз в истории разговора
2. Реализовать динамическую адаптацию техник на основе реакции пользователя
3. Добавить анализ эффективности каждой техники поддержки
4. Внедрить A/B тестирование разных подходов к генерации хинтов
