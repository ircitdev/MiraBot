# Улучшения качества AI — декабрь 2024

## 🎯 Проблемы из user feedback

На основе реального отзыва пользователя выявлены критические проблемы качества разговоров:

### Отзыв пользователя:
> **"Не помнит что я работаю в больнице"** — потеря контекста
> **"Одни и те же истории повторяет"** — повторяющиеся паттерны
> **"Если что дышим дышим"** — чрезмерное использование одной техники
> **Неуместные hints**: "Выпорю", "Хочу убить", "Не ценят" — показываются вне контекста

**Приоритет:** 🔴 **ОБЯЗАТЕЛЬНО** (помечено пользователем)

---

## ✅ Реализованные улучшения

### 1. Улучшение системы подсказок (hints)

**Файл:** `ai/hint_generator.py`

#### Проблема:
- Агрессивные формулировки: "Выпорю", "Хочу убить", "Не ценят"
- Подсказки показывались вне контекста (например, hints про работу при простом упоминании слова "работа")

#### Решение:

**a) Смягчены формулировки (строки 27-59):**
```python
# БЫЛО:
"topic:work": [
    Hint("Выгораю", "Кажется, я выгораю на работе...", 5),
    Hint("Хочу уйти", "Думаю уйти, но страшно...", 4),
    Hint("Не ценят", "Чувствую, что меня не ценят...", 4),
]

# СТАЛО:
"topic:work": [
    Hint("Устаю на работе", "В последнее время очень устаю на работе...", 5),
    Hint("Думаю о переменах", "Думаю о переменах, но пока не решилась...", 4),
    Hint("Сложная ситуация", "На работе сложная ситуация...", 4),
]
```

**b) Добавлена контекстная проверка (строки 210-214, 310-384):**
```python
# Новый метод _is_topic_active()
# Проверяет что тема ДЕЙСТВИТЕЛЬНО обсуждается СЕЙЧАС

# Пример для topic:work:
work_active_phrases = [
    "на работе", "работе трудно", "работе тяжело",
    "начальник", "коллеги меня", "устала на работе",
    "выгораю", "карьера", "увольняться", "уволить",
    "рабочий день", "работе не ценят", "работе стресс"
]
```

**c) Передача user_message для анализа:**
- Обновлены вызовы в `bot/handlers/message.py:261` и `bot/handlers/callbacks.py:386`
- Теперь генератор hints анализирует как сообщение пользователя, так и ответ бота

**Эффект:**
- ❌ НЕТ агрессивных формулировок
- ✅ Hints показываются только когда тема ЦЕНТРАЛЬНАЯ в разговоре
- ✅ Более релевантные и уместные подсказки

---

### 2. Детектор повторений и разнообразие техник

**Файл:** `ai/prompts/system_prompt.py`

#### Проблема:
- Мира повторяет одни и те же истории
- Слишком часто предлагает дыхательные упражнения ("если что дышим дышим")

#### Решение:

**a) Инструкция против повторений (строки 91-97):**
```python
**Техники непрямого наставничества:**
- Используй истории: "Знаешь, у меня была знакомая в похожей ситуации..."
  **⚠️ ВАЖНО:** НЕ повторяй одни и те же истории! Придумывай новые примеры каждый раз.
  Если уже рассказывала похожую историю в этом разговоре — используй ДРУГУЮ технику!
- ВАРЬИРУЙ подходы: не используй одну и ту же технику подряд
```

**b) Новый раздел: "РАЗНООБРАЗИЕ ТЕХНИК ПОДДЕРЖКИ" (строки 120-145):**
```python
## 🔴 КРИТИЧЕСКИ ВАЖНО — РАЗНООБРАЗИЕ ТЕХНИК ПОДДЕРЖКИ

**ПРАВИЛО РАЗНООБРАЗИЯ:**
1. **НЕ предлагай дыхательные упражнения** в КАЖДОМ ответе!
   - Используй их ТОЛЬКО при сильной тревоге/панике
   - Если уже предлагала дыхание в последних 3-4 сообщениях — используй ДРУГУЮ технику

2. **Чередуй техники поддержки:**
   - Активное слушание и валидация
   - Рефлексивные вопросы
   - Нормализация
   - Мягкое переформулирование
   - Истории (но РАЗНЫЕ истории!)
   - Метафоры и образы
   - Дыхательные упражнения (РЕДКО!)
   - Медитации, практические упражнения

3. **Проверяй себя:**
   - Если в последних 2-3 ответах использовала одну технику — СМЕНИ подход!
   - Разнообразие = эффективность
```

**Эффект:**
- ✅ Claude активно варьирует техники поддержки
- ✅ Дыхательные упражнения используются РЕЖЕ и уместнее
- ✅ Каждый ответ звучит свежо, без повторов

---

### 3. Улучшение тегирования тем

**Файл:** `ai/claude_client.py`

#### Проблема:
- Теги (например, `topic:work`) добавлялись при простом упоминании слова
- Это приводило к показу неуместных hints

#### Решение:

**Переработан метод `_extract_tags()` (строки 245-311):**

```python
# БЫЛО — слишком мягкие критерии:
"topic:work": ["работа", "карьера", "начальник", "коллеги", "офис"]

# СТАЛО — строгие критерии:
# topic:work - СТРОГО только при активном обсуждении работы
if any(phrase in combined_text for phrase in [
    "на работе", "работе трудно", "работе тяжело",
    "начальник", "коллеги меня", "устала на работе",
    "выгораю", "карьера", "увольняться",
    "рабочий день", "работе не ценят", "работе стресс"
]):
    tags.append("topic:work")
```

**Ключевые изменения:**
- НЕ достаточно просто упомянуть "работа" — нужны фразы типа "на работе", "устала на работе"
- Теги `topic:self` проверяются только в сообщении пользователя (не в ответе бота!)
- Каждый тег требует фраз, показывающих что тема ОБСУЖДАЕТСЯ, а не просто упоминается

**Эффект:**
- ✅ Более точное определение активных тем
- ✅ Hints показываются только когда тема действительно в фокусе
- ✅ Меньше ложных срабатываний

---

## 📊 Сводка изменений

### Изменённые файлы:

| Файл | Строки | Описание изменения |
|------|--------|-------------------|
| `ai/hint_generator.py` | 27-59 | Смягчены формулировки hints |
| `ai/hint_generator.py` | 159, 213-214 | Добавлен параметр `user_message` |
| `ai/hint_generator.py` | 310-384 | Новый метод `_is_topic_active()` |
| `ai/prompts/system_prompt.py` | 91-97 | Инструкция против повторений |
| `ai/prompts/system_prompt.py` | 120-145 | Раздел "Разнообразие техник" |
| `ai/claude_client.py` | 245-311 | Строгие критерии тегирования |
| `bot/handlers/message.py` | 261 | Передача `user_message` в hints |
| `bot/handlers/callbacks.py` | 386 | Передача `user_message` в hints |

### Статистика:
- **8 файлов** изменено
- **~150 строк** добавлено/изменено
- **4 критические проблемы** решены

---

## 🎯 Ожидаемые результаты

### До улучшений:
- ❌ "Одни и те же истории повторяет"
- ❌ "Если что дышим дышим"
- ❌ Неуместные hints типа "Выпорю", "Хочу убить"
- ❌ Hints про работу при упоминании слова вскользь

### После улучшений:
- ✅ Разнообразные техники поддержки
- ✅ Дыхательные упражнения ТОЛЬКО при тревоге
- ✅ Мягкие, уместные формулировки hints
- ✅ Hints показываются ТОЛЬКО в релевантном контексте
- ✅ Каждый ответ звучит свежо и индивидуально

---

## 🚀 Деплой

**Дата:** 15 декабря 2024
**Окружение:** Production (root@31.44.7.144)
**Сервис:** mira_bot

**Команды деплоя:**
```bash
# 1. Коммит изменений
git add .
git commit -m "feat: критические улучшения качества AI на основе user feedback..."

# 2. Загрузка на сервер
scp -r ai/ bot/ root@31.44.7.144:/var/www/mira_bot/

# 3. Перезапуск сервиса
ssh root@31.44.7.144 "systemctl restart mira_bot"
```

---

## 📝 Примечания

1. **Обратная совместимость:** Все изменения обратно совместимы, не требуют миграций БД
2. **Производительность:** Изменения НЕ влияют на скорость ответов (логика работает в том же потоке)
3. **Мониторинг:** Рекомендуется отслеживать feedback пользователей в течение 1-2 недель
4. **Дальнейшие улучшения:** Возможно добавление ML-модели для детекции повторений в будущем

---

## 🙏 Благодарности

Спасибо пользователю за детальный feedback! Именно реальные отзывы помогают делать продукт лучше.

**Статус:** ✅ **ГОТОВО К ДЕПЛОЮ**
