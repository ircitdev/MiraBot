# Исправление логирования действий администраторов

**Дата:** 30.12.2025
**Проблема:** Лог операций пустой, ошибка сериализации Pydantic моделей

## 🔍 Причина

Логирование действий администраторов падало с ошибкой:

```
Failed to log admin action: (builtins.TypeError) Object of type CreateReportRequest is not JSON serializable
```

### Что происходило:

1. Администратор выполняет действие (например, создаёт отчёт)
2. Декоратор `@log_admin_action` пытается сохранить параметры запроса в БД
3. Один из параметров - Pydantic модель `CreateReportRequest`
4. Попытка сохранить её напрямую в JSON поле БД → **TypeError**
5. Логирование падает, запись **НЕ сохраняется** в БД
6. Лог операций остаётся пустым

### Код с ошибкой:

**Файл:** `webapp/api/decorators.py`

```python
# БЫЛО (неправильно):
safe_kwargs = {
    k: v for k, v in kwargs.items()
    if k not in ["admin_data", "request", "password", "token"]
    and not k.startswith("_")
}
if safe_kwargs:
    details["params"] = safe_kwargs  # ← Ошибка, если v - Pydantic модель
```

Проблема: если `v` - это Pydantic модель, её нельзя просто положить в словарь, который потом идёт в JSON.

## ✅ Исправление

Добавлена умная сериализация параметров:

```python
# СТАЛО (правильно):
safe_kwargs = {}
for k, v in kwargs.items():
    if k not in ["admin_data", "request", "password", "token"] and not k.startswith("_"):
        # Сериализуем Pydantic модели в dict
        if hasattr(v, "model_dump"):
            safe_kwargs[k] = v.model_dump()
        elif hasattr(v, "dict"):
            safe_kwargs[k] = v.dict()
        elif isinstance(v, (str, int, float, bool, type(None))):
            safe_kwargs[k] = v
        elif isinstance(v, (list, dict)):
            safe_kwargs[k] = v
        else:
            # Для остальных типов сохраняем строковое представление
            safe_kwargs[k] = str(v)

if safe_kwargs:
    details["params"] = safe_kwargs
```

### Как это работает:

1. **Pydantic v2** (новый): `v.model_dump()` → конвертирует модель в dict
2. **Pydantic v1** (старый): `v.dict()` → конвертирует модель в dict
3. **Простые типы**: `str, int, float, bool, None` → сохраняются как есть
4. **Списки и словари**: сохраняются как есть
5. **Остальное**: конвертируется в строку через `str(v)`

## 🔧 Что было исправлено

### 1. Декоратор `@log_admin_action` (строки 94-112)

Используется для обычных действий администраторов:
- Создание отчётов
- Выдача подписок
- Разблокировка пользователей
- И т.д.

Если логирование падает - просто игнорируется (не ломает основную функциональность).

### 2. Декоратор `@log_critical_action` (строки 184-203)

Используется для критических действий:
- Удаление пользователей
- Блокировка пользователей
- Удаление сообщений
- И т.д.

Логирование происходит ДО выполнения действия. Если падает - действие не выполняется.

## 📊 Результат

После исправления логирование работает корректно:

### До исправления:
```sql
SELECT COUNT(*) FROM admin_logs;
-- Результат: 0 (логов нет)
```

```
journalctl -u mira-webapp | grep "Failed to log admin action"
-- Результат: множество ошибок
Failed to log admin action: (builtins.TypeError) Object of type CreateReportRequest is not JSON serializable
```

### После исправления:

✅ Логирование работает без ошибок
✅ При выполнении действий администратором создаются записи в `admin_logs`
✅ Лог операций начнёт заполняться автоматически

## 🎯 Когда появятся логи?

Логи будут создаваться автоматически когда администраторы:

1. **Создают отчёт пользователю** (`report_create`)
2. **Блокируют/разблокируют пользователя** (`user_block`, `user_unblock`)
3. **Выдают подписку** (`subscription_grant`)
4. **Удаляют сообщения** (`message_delete`)
5. **Изменяют настройки** (`settings_update`)

### Пример записи лога:

```json
{
  "id": 1,
  "admin_user_id": 2,
  "action": "report_create",
  "resource_type": "report",
  "resource_id": null,
  "details": {
    "params": {
      "telegram_id": 1392513515,
      "data": {
        "content": "Текст отчёта...",
        "tokens_used": 9586,
        "cost_usd": 0.03591
      }
    },
    "success": true
  },
  "ip_address": "103.109.234.5",
  "user_agent": "Mozilla/5.0 ...",
  "success": true,
  "error_message": null,
  "created_at": "2025-12-30T08:09:12"
}
```

## 📋 Что было сделано

1. ✅ Исправлен декоратор `@log_admin_action` - добавлена сериализация Pydantic моделей
2. ✅ Исправлен декоратор `@log_critical_action` - та же проблема
3. ✅ Файл загружен на сервер
4. ✅ Веб-сервер перезапущен

## 🔍 Проверка

### Проверить что ошибок больше нет:

```bash
ssh root@31.44.7.144
journalctl -u mira-webapp -f | grep "Failed to log admin action"
# Не должно быть ошибок
```

### Проверить что логи создаются:

1. Откройте админ-панель
2. Выполните любое действие (например, создайте отчёт пользователю)
3. Откройте раздел "Контекст" → вкладка с логами
4. Должна появиться запись о выполненном действии

### Через базу данных:

```bash
ssh root@31.44.7.144
cd /root/mira_bot
python3 -c "
import asyncio
from database.repositories.admin_log import AdminLogRepository

async def check():
    repo = AdminLogRepository()
    logs = await repo.list_logs(limit=10)
    print(f'Total logs: {len(logs)}')
    for log in logs:
        print(f'  - {log.action} at {log.created_at}')

asyncio.run(check())
"
```

## 📄 Файлы изменений

- **`webapp/api/decorators.py`** (строки 94-112, 184-203) - исправлена сериализация параметров

## 🎉 Итог

✅ **Логирование теперь работает!**

После выполнения любого действия администратором в разделе "Контекст" появится запись в логе операций.

**Почему лог был пустой?**
- Логирование падало с ошибкой при попытке сохранить Pydantic модели
- Теперь модели корректно сериализуются в dict перед сохранением

**Что нужно сделать?**
- Ничего! Логирование будет работать автоматически
- Просто выполните любое действие через админ-панель и проверьте лог
