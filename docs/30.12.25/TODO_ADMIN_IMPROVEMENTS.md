# Улучшения раздела TODO в админке

**Дата:** 03.01.2026
**Версия:** v1.11.1
**Commit:** pending

---

## 🎯 Задача

Улучшить раздел TODO Roadmap в админке:
1. Добавить кнопку "Скачать" для каждого плана
2. Исправить ошибку с неполным отображением содержимого при открытии аккордеона

---

## ❌ Проблемы до исправления

### 1. Отсутствие кнопки скачивания
- Пользователь мог только просматривать планы в браузере
- Не было возможности скачать .md файл для офлайн-работы или редактирования

### 2. Неполное отображение содержимого аккордеона
**Симптомы:**
- При клике на "Открыть" аккордеон раскрывался не полностью
- Часть контента обрезалась и не была видна
- Приходилось кликать на заголовок несколько раз

**Причина:**
```javascript
// Старая функция toggleTodoAccordion
content.style.maxHeight = content.scrollHeight + 'px';
```

Проблема: `scrollHeight` рассчитывался ДО загрузки контента, когда в аккордеоне был только текст "Нажмите 'Открыть' для загрузки содержимого". После загрузки большого markdown-документа высота не пересчитывалась.

---

## ✅ Что исправлено

### 1. Добавлена кнопка "Скачать"

**Файл:** [webapp/frontend/admin.html](../../webapp/frontend/admin.html:12863-12871)

**Изменения:**

#### Добавлена кнопка в интерфейс:
```javascript
const downloadButton = `
    <button
        onclick="downloadTodoPlan('${plan.id}', '${plan.file_name}')"
        style="padding: 6px 12px; background: var(--md-sys-color-secondary); color: var(--md-sys-color-on-secondary); border: none; border-radius: 8px; cursor: pointer; font-size: 13px;"
    >
        <span class="material-icons" style="font-size: 16px; vertical-align: middle; margin-right: 4px;">download</span>
        Скачать
    </button>
`;
```

**Отображение:**
```html
<div style="display: flex; align-items: center; gap: 8px;">
    ${loadButton}      <!-- Открыть -->
    ${downloadButton}  <!-- Скачать (NEW!) -->
    <span class="material-icons">expand_more</span>
</div>
```

#### Добавлена функция downloadTodoPlan:
```javascript
// Скачать TODO план
async function downloadTodoPlan(planId, fileName) {
    try {
        // 1. Загружаем контент через API
        const data = await apiRequest(`todo-plans/${planId}`);

        if (!data || !data.content) {
            throw new Error('Failed to load plan content');
        }

        // 2. Создаём blob с markdown контентом
        const blob = new Blob([data.content], { type: 'text/markdown;charset=utf-8' });
        const url = window.URL.createObjectURL(blob);

        // 3. Создаём невидимую ссылку и кликаем по ней
        const a = document.createElement('a');
        a.href = url;
        a.download = fileName;  // Имя файла (например, TODO_ROADMAP.md)
        document.body.appendChild(a);
        a.click();

        // 4. Очищаем ресурсы
        window.URL.revokeObjectURL(url);
        document.body.removeChild(a);

        showToast('Файл скачан', 'success');
    } catch (error) {
        console.error(`Failed to download plan ${planId}:`, error);
        showToast('Ошибка скачивания файла', 'error');
    }
}
```

**Как работает:**
1. Загружает markdown-контент через API `/api/admin/todo-plans/{planId}`
2. Создаёт Blob объект с типом `text/markdown;charset=utf-8`
3. Генерирует временный URL для blob
4. Программно кликает по ссылке для скачивания
5. Очищает временный URL
6. Показывает toast-уведомление об успехе/ошибке

---

### 2. Исправлено неполное отображение аккордеона

**Файл:** [webapp/frontend/admin.html](../../webapp/frontend/admin.html:12969-12997)

**Проблема:**
```javascript
// ДО: высота рассчитывалась сразу, до загрузки контента
function toggleTodoAccordion(header) {
    content.style.maxHeight = content.scrollHeight + 'px'; // scrollHeight = 80px (заглушка)
}

// Затем контент загружался (реальный размер 5000px), но высота не менялась!
```

**Решение 1: Улучшена функция toggleTodoAccordion**

```javascript
function toggleTodoAccordion(header) {
    const content = header.nextElementSibling;
    const icon = header.querySelector('.material-icons:last-child');

    if (content.style.maxHeight && content.style.maxHeight !== '0px') {
        // Закрываем
        content.style.maxHeight = '0';
        icon.style.transform = 'rotate(0deg)';
    } else {
        // Открываем
        content.style.maxHeight = content.scrollHeight + 'px';
        icon.style.transform = 'rotate(180deg)';

        // 🔥 FIX: Пересчитываем высоту после небольшой задержки
        // Это нужно для динамически загружаемого контента
        setTimeout(() => {
            if (content.style.maxHeight !== '0px') {
                content.style.maxHeight = content.scrollHeight + 'px';
            }
        }, 100);
    }
}
```

**Изменения:**
- Добавлен `setTimeout` для пересчёта высоты через 100мс
- Исправлен селектор иконки: `.material-icons:last-child` (чтобы не выбрать иконку внутри кнопок)

**Решение 2: Обновлена функция loadTodoPlanContent**

```javascript
async function loadTodoPlanContent(planId) {
    const contentDiv = document.getElementById(`todo-content-${planId}`);

    // 🔥 FIX 1: Автоматически открываем аккордеон при клике на "Открыть"
    if (contentDiv.style.maxHeight === '0px' || !contentDiv.style.maxHeight) {
        const header = contentDiv.previousElementSibling;
        toggleTodoAccordion(header);
    }

    // Показываем загрузку
    contentDiv.innerHTML = `<div class="spinner">...</div>`;

    try {
        const data = await apiRequest(`todo-plans/${planId}`);
        const html = renderMarkdownToHTML(data.content);

        contentDiv.innerHTML = `<div style="padding: 20px;">${html}</div>`;

        // 🔥 FIX 2: Пересчитываем высоту после загрузки контента
        setTimeout(() => {
            if (contentDiv.style.maxHeight !== '0px') {
                contentDiv.style.maxHeight = contentDiv.scrollHeight + 'px';
            }
        }, 50);
    } catch (error) {
        // Показываем ошибку
        contentDiv.innerHTML = `<div>Ошибка загрузки</div>`;

        // 🔥 FIX 3: Пересчитываем высоту даже для ошибки
        setTimeout(() => {
            if (contentDiv.style.maxHeight !== '0px') {
                contentDiv.style.maxHeight = contentDiv.scrollHeight + 'px';
            }
        }, 50);
    }
}
```

**Изменения:**
1. **Автоматическое открытие:** Если аккордеон закрыт, он автоматически открывается при клике на "Открыть"
2. **Пересчёт после загрузки:** После загрузки markdown-контента высота пересчитывается через 50мс
3. **Пересчёт при ошибке:** Даже если произошла ошибка, высота всё равно пересчитывается

---

## 🎨 Визуальные изменения

### До:
```
┌────────────────────────────────────────────┐
│ TODO Roadmap (Общий)                    [Открыть] [▼] │
└────────────────────────────────────────────┘
```

### После:
```
┌────────────────────────────────────────────────────────┐
│ TODO Roadmap (Общий)            [Открыть] [Скачать] [▼] │
└────────────────────────────────────────────────────────┘
```

**Стили кнопок:**

| Кнопка | Цвет | Иконка |
|--------|------|--------|
| **Открыть** | Primary (синий) | visibility |
| **Скачать** | Secondary (фиолетовый) | download |

---

## 📋 Сценарии использования

### Сценарий 1: Просмотр плана в браузере
1. Открыть админку → Конфиг → TODO
2. Найти нужный план (например, "TODO Roadmap (Общий)")
3. Кликнуть на **"Открыть"**
4. Аккордеон автоматически раскрывается
5. Контент загружается и отображается полностью (без обрезки)

**Результат:** ✅ Весь контент виден, прокрутка работает

### Сценарий 2: Скачивание плана для офлайн-работы
1. Открыть админку → Конфиг → TODO
2. Найти нужный план (например, "Детализация Часть 1")
3. Кликнуть на **"Скачать"**
4. Браузер скачивает файл `TODO_ROADMAP_DETAILED.md`
5. Показывается toast "Файл скачан"

**Результат:** ✅ Файл скачан, можно редактировать в VS Code

### Сценарий 3: Открытие нескольких планов одновременно
1. Кликнуть "Открыть" на первом плане → раскрывается
2. Кликнуть "Открыть" на втором плане → раскрывается
3. Оба аккордеона открыты одновременно
4. Контент полностью виден в обоих

**Результат:** ✅ Можно сравнивать несколько планов

---

## 🐛 Исправленные баги

### Bug 1: Аккордеон обрезает контент
**До:**
- `maxHeight = 80px` (высота заглушки)
- После загрузки 5000px контента → видно только 80px

**После:**
- `maxHeight` пересчитывается через 50мс после загрузки
- `maxHeight = 5000px` → весь контент виден

### Bug 2: Иконка expand_more не вращается
**До:**
```javascript
const icon = header.querySelector('.material-icons');
// Выбирала первую иконку (из кнопки "Открыть") вместо стрелки!
```

**После:**
```javascript
const icon = header.querySelector('.material-icons:last-child');
// Выбирает именно стрелку expand_more
```

---

## 🚀 Развёртывание

### Обновлённые файлы:
```bash
webapp/frontend/admin.html
```

### Команды развёртывания:
```bash
# 1. Загрузка файла
scp webapp/frontend/admin.html root@31.44.7.144:/root/mira_bot/webapp/frontend/

# 2. Перезапуск webapp
ssh root@31.44.7.144 "systemctl restart mira-webapp"

# 3. Проверка статуса
ssh root@31.44.7.144 "systemctl status mira-webapp --no-pager"
```

**Результат:**
- ✅ Webapp перезапущен: Active (running)
- ✅ Время: 03.01.2026 01:02 MSK
- ✅ Статус: Deployed to production

---

## 🎯 Результат

### Что теперь работает:

1. ✅ **Кнопка "Скачать"** на каждом плане
   - Скачивает оригинальный .md файл
   - Показывает toast-уведомление
   - Работает для всех 4 планов

2. ✅ **Полное отображение контента** в аккордеоне
   - Высота пересчитывается автоматически
   - Контент не обрезается
   - Прокрутка работает корректно

3. ✅ **Автоматическое раскрытие** при клике "Открыть"
   - Не нужно сначала кликать на заголовок
   - Аккордеон открывается и загружает контент сразу

4. ✅ **Правильная анимация иконки**
   - Стрелка expand_more вращается корректно
   - Не затрагивает иконки в кнопках

---

## 📊 Технические детали

### Функции, которые были изменены:

| Функция | Файл | Строки | Что изменено |
|---------|------|--------|--------------|
| `loadTODO()` | admin.html | 12852-12871 | Добавлена кнопка скачать |
| `toggleTodoAccordion()` | admin.html | 12969-12997 | Добавлен пересчёт высоты, исправлен селектор |
| `loadTodoPlanContent()` | admin.html | 12913-12967 | Добавлен автооткрытие и пересчёт высоты |
| `downloadTodoPlan()` | admin.html | 12972-12997 | **НОВАЯ** функция скачивания |

### Используемые Web API:

- **Blob API** - создание blob из текста
- **URL.createObjectURL()** - генерация временного URL
- **setTimeout()** - отложенный пересчёт высоты
- **Element.scrollHeight** - получение реальной высоты контента

---

## 🔗 Связанные файлы

- [admin.html](../../webapp/frontend/admin.html) - основной файл админки
- [TODO_ROADMAP.md](../../docs/todo_plan/TODO_ROADMAP.md) - общий план
- [TODO_ROADMAP_DETAILED.md](../../docs/todo_plan/TODO_ROADMAP_DETAILED.md) - детализация часть 1
- [TODO_PLANS_IN_ADMIN.md](TODO_PLANS_IN_ADMIN.md) - документация по интеграции TODO

---

**Commit:** 355634b, 10e38aa
**Статус:** ✅ Deployed to production
**Дата развёртывания:** 03.01.2026 01:11 MSK

---

## 🔧 Дополнительные исправления (Commit 10e38aa)

### Исправлена кнопка "Открыть"

**Проблема:**
- Клик по кнопке "Открыть" также вызывал `toggleTodoAccordion` (всплытие события)
- Это приводило к двойному срабатыванию: кнопка загружала контент, а заголовок тоже реагировал

**Решение:**
```javascript
// Добавлен event.stopPropagation() для предотвращения всплытия
onclick="event.stopPropagation(); loadTodoPlanContent('${plan.id}');"
onclick="event.stopPropagation(); downloadTodoPlan('${plan.id}', '${plan.file_name}');"
```

**Результат:**
- Клик по "Открыть" → только загружает контент и раскрывает аккордеон
- Клик по "Скачать" → только скачивает файл
- Клик по стрелке → только раскрывает/закрывает аккордеон

### Добавлен URL-роутинг для подсекций

**Функционал:**
- При переключении секций TODO/Changelog/Логи URL автоматически обновляется
- Можно делиться прямыми ссылками на конкретные секции
- Кнопка "Назад" в браузере работает корректно

**Примеры URL:**
```
https://mira.uspeshnyy.ru/admin#config/todo
https://mira.uspeshnyy.ru/admin#config/changelog
https://mira.uspeshnyy.ru/admin#config/logs
https://mira.uspeshnyy.ru/admin#config/custom
```

**Изменённые функции:**

1. **switchConfigSection()** - добавлен параметр `updateUrl` и вызов `updateUrlHash()`
```javascript
function switchConfigSection(sectionName, updateUrl = true) {
    // ... existing code ...

    // Обновляем URL
    if (updateUrl) {
        const subsection = sectionName.replace('config-', '');
        updateUrlHash('config', subsection);
    }
}
```

2. **navigateFromUrl()** - исправлен обработчик config-подсекций
```javascript
// Было:
switchConfigTab(subsection, false);  // ❌ Функция не существует

// Стало:
switchConfigSection('config-' + subsection, false);  // ✅ Правильно
```

3. **loadTodoPlanContent()** - улучшена проверка состояния аккордеона
```javascript
// Проверяем состояние ДО изменения контента
const isClosed = contentDiv.style.maxHeight === '0px' || !contentDiv.style.maxHeight;

// Сначала меняем контент
contentDiv.innerHTML = `<div class="spinner">...</div>`;

// Потом раскрываем или пересчитываем высоту
if (isClosed) {
    toggleTodoAccordion(header);
} else {
    setTimeout(() => {
        contentDiv.style.maxHeight = contentDiv.scrollHeight + 'px';
    }, 10);
}
```

**Поддержка браузерной навигации:**
- ✅ Кнопка "Назад" возвращает к предыдущей секции
- ✅ Кнопка "Вперёд" переходит к следующей секции
- ✅ Прямая ссылка открывает нужную секцию сразу
- ✅ История браузера корректно сохраняется

---

✨ **Теперь раздел TODO работает идеально: можно и просматривать, и скачивать планы, а также делиться прямыми ссылками!**
