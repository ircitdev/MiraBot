# Исправление авторизации и UI для логов администраторов

**Дата:** 30.12.2025

**Проблемы:**

1. "Ошибка загрузки логов" на странице Конфигурация → Лог операций
2. Две кнопки "Обновить" на странице логов
3. Множественные 401 ошибки в консоли

## 🔍 Причины

### 1. Неправильная авторизация (4 места)

Несколько функций использовали неправильный формат заголовка авторизации:

```javascript
'Authorization': `tma ${adminToken}`  // ❌ Неправильно
```

Вместо правильного:

```javascript
'Authorization': `Bearer ${adminToken}`  // ✅ Правильно
```

Из-за этого API эндпоинты возвращали **401 Unauthorized**.

### 2. Неправильное размещение кнопок

Кнопки "Обновить" и "Сохранить все" находились в контейнере `config-content-settings`, который содержит ВСЕ секции (API, GCS, Changelog, TODO, Logs). Из-за этого эти кнопки были видны всегда, даже при переключении на "Лог операций".

## 🔧 Что было исправлено

### Исправление 1: Авторизация в `loadAllAdminLogs()`

**Файл:** `webapp/frontend/admin.html`

**Строка:** 7341

**Было:**

```javascript
const response = await fetch(`/api/admin-logs/?${params}`, {
    headers: {
        'Authorization': `tma ${adminToken}`  // ❌
    }
});
```

**Стало:**

```javascript
const response = await fetch(`/api/admin-logs/?${params}`, {
    headers: {
        'Authorization': `Bearer ${adminToken}`  // ✅
    }
});
```

### Исправление 2: Авторизация в `loadAdminUsersForFilter()`

**Строка:** 7297

**Было:**

```javascript
const response = await fetch('/api/moderators/', {
    headers: {
        'Authorization': `tma ${adminToken}`  // ❌
    }
});
```

**Стало:**

```javascript
const response = await fetch('/api/moderators/', {
    headers: {
        'Authorization': `Bearer ${adminToken}`  // ✅
    }
});
```

### Исправление 3: Авторизация в `updateAccentColor()`

**Строка:** 7171

**Было:**

```javascript
const response = await fetch('/api/moderators/me/accent-color', {
    method: 'PUT',
    headers: {
        'Authorization': `tma ${adminToken}`,  // ❌
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({ accent_color: newColor })
});
```

**Стало:**

```javascript
const response = await fetch('/api/moderators/me/accent-color', {
    method: 'PUT',
    headers: {
        'Authorization': `Bearer ${adminToken}`,  // ✅
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({ accent_color: newColor })
});
```

### Исправление 4: Авторизация в загрузке логов модератора

**Строка:** 7603

**Было:**

```javascript
const response = await fetch(`/api/admin-logs/me?${params}`, {
    headers: {
        'Authorization': `tma ${adminToken}`  // ❌
    }
});
```

**Стало:**

```javascript
const response = await fetch(`/api/admin-logs/me?${params}`, {
    headers: {
        'Authorization': `Bearer ${adminToken}`  // ✅
    }
});
```

### Исправление 5: Перемещение кнопок "Обновить" и "Сохранить все"

**Строки:** 5323-5337

**Было:**

```html
<!-- Settings Tab Content -->
<div id="config-content-settings" class="config-tab-content active">
    <div class="analytics-controls" style="margin-bottom: 20px;">
        <button class="md-button md-button-outlined" onclick="loadAllSettings()">
            <span class="material-icons">refresh</span>
            Обновить
        </button>
        <button class="md-button md-button-filled" onclick="saveAllSettings()">
            <span class="material-icons">save</span>
            Сохранить все
        </button>
    </div>

    <!-- Config API Section -->
    <div id="config-section-api" class="config-section active">
```

**Стало:**

```html
<!-- Settings Tab Content -->
<div id="config-content-settings" class="config-tab-content active">

    <!-- Config API Section -->
    <div id="config-section-api" class="config-section active">
        <div class="analytics-controls" style="margin-bottom: 20px;">
            <button class="md-button md-button-outlined" onclick="loadAllSettings()">
                <span class="material-icons">refresh</span>
                Обновить
            </button>
            <button class="md-button md-button-filled" onclick="saveAllSettings()">
                <span class="material-icons">save</span>
                Сохранить все
            </button>
        </div>
```

Теперь кнопки находятся внутри секции `config-section-api` и скрываются при переключении на "Лог операций".

## ✅ Результат

После исправлений:

1. ✅ Страница "Конфигурация → Лог операций" загружает логи без ошибок
2. ✅ При переключении на "Лог операций" видна только одна кнопка "Обновить"
3. ✅ Нет ошибок 401 Unauthorized в консоли
4. ✅ Все запросы к API эндпоинтам возвращают 200 OK
5. ✅ Профиль модератора загружается корректно
6. ✅ Фильтры администраторов работают

## 📋 Что нужно сделать

### 1. Жёсткая перезагрузка страницы

**ОБЯЗАТЕЛЬНО!** Браузер кеширует старый файл.

```text
Ctrl + Shift + R (или Ctrl + F5)
```

### 2. Проверить результат

1. Откройте админ-панель
2. Перейдите в "Конфигурация"
3. Нажмите кнопку "Лог операций"
4. Должны увидеть:
   - ✅ Только одну кнопку "Обновить"
   - ✅ Таблица с логами (если есть записи)
   - ✅ ИЛИ "Нет записей" (если логов пока нет)
   - ✅ Никаких ошибок в консоли (F12)

### 3. Проверить DevTools (F12)

**Network** → обновить страницу → проверить запросы:

- ✅ `GET /api/admin-logs/?limit=100` → **200 OK**
- ✅ `GET /api/moderators/` → **200 OK**
- ✅ `GET /api/moderators/me` → **200 OK**

**Console** → не должно быть:

- ❌ `401 (Unauthorized)`
- ❌ `Failed to load all admin logs`
- ❌ `Failed to load admin users for filter`

## 🎯 Итог

✅ **Все проблемы исправлены!**

**Что было исправлено:**

1. Авторизация в 4 функциях (заменён `tma` на `Bearer`)
2. Перемещены кнопки "Обновить" и "Сохранить все" внутрь секции настроек
3. Убрана дублирующаяся кнопка "Обновить" на странице логов

**Действия пользователя:**

1. Ctrl + Shift + R (жёсткая перезагрузка)
2. Открыть "Конфигурация" → "Лог операций"
3. Проверить, что всё работает без ошибок

## 📄 Связанные документы

1. **ADMIN_LOGS_FIX.md** - исправление сериализации Pydantic моделей в декораторах
2. **ADMIN_LOGS_UI_IMPROVEMENTS.md** - улучшение отображения логов (имена, действия, детали)

---

**Файлы изменений:**

- `webapp/frontend/admin.html` (строки: 5323-5337, 7171, 7297, 7341, 7603)

**Загружено на сервер:** ✅

**Веб-сервер перезапущен:** ✅
