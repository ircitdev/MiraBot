# Документация изменений от 30.12.2025

Все доработки Фазы 10: API Аналитика и Управление

---

## 📚 Содержание документации

### 1. [OPENAI_WHISPER_API_COST_LOGGING.md](OPENAI_WHISPER_API_COST_LOGGING.md)
**Тема:** Логирование расходов на OpenAI Whisper API

**Что описано:**
- Зачем нужно логирование Whisper API
- Изменения в `WhisperClient.transcribe_bytes()`
- Обновление обработчика голосовых сообщений
- Формат сохранения в БД
- Расчёт стоимости ($0.0001 за секунду)
- Примеры логов и запросов к БД
- Проверка работы функции

**Ключевые изменения:**
- Метод теперь возвращает tuple: `(text, cost_info)`
- Автоматический расчёт стоимости
- Сохранение в таблицу `api_costs`
- Логирование каждой транскрибации

---

### 2. [API_COSTS_DETAILS_ENDPOINT_FIX.md](API_COSTS_DETAILS_ENDPOINT_FIX.md)
**Тема:** Исправление эндпоинта детальной статистики API

**Что описано:**
- Причина ошибки 404 Not Found
- Добавление эндпоинта `GET /api-costs/`
- Реализация метода `get_costs_list()` в репозитории
- Исправление `get_top_users_by_cost()` (добавлены first_name и provider)
- Почему OpenAI не показывался (нет данных в БД)
- Проверка через curl и браузер

**Ключевые изменения:**
- Новый эндпоинт для получения списка транзакций
- Поддержка фильтров: даты, telegram_id, провайдер
- Пагинация: limit + offset
- Полная информация о каждой транзакции

---

### 3. [SYSTEM_PROMPT_UPLOAD_FEATURE.md](SYSTEM_PROMPT_UPLOAD_FEATURE.md)
**Тема:** Добавлена возможность загрузки System Prompt через админ-панель

**Что описано:**
- Зачем нужна загрузка через UI
- Добавлена кнопка "Загрузить новый PROMPT"
- Реализация функции `uploadNewPrompt()`
- Создание API эндпоинта `POST /system-prompt/update`
- Исправление ошибки двойного `.json()`
- Инструкция по использованию
- Безопасность и валидация
- Автоматические бэкапы

**Ключевые изменения:**
- Кнопка загрузки в секции SYSTEM PROMPT
- JavaScript функция для выбора файла
- Backend endpoint с валидацией
- Автоматическое создание бэкапов в `ai/prompts/backups/`
- Логирование действий администратора
- Исправлены все функции с двойным `.json()`

---

### 4. [PHASE_10_SUMMARY.md](PHASE_10_SUMMARY.md)
**Тема:** Полная сводка Фазы 10: API Аналитика и Управление

**Что описано:**
- Обзор всех 7 новых функций
- Детальное описание каждого изменения
- Примеры кода (до/после)
- Структура файлов проекта
- Workflow использования
- Метрики и аналитика
- Безопасность
- Known Issues
- Критерии приёмки

**Охватывает:**
- Функцию #30: Логирование расходов OpenAI Whisper API
- Функцию #31: Раздел "Расходы API" в админке
- Функцию #32: Новые API эндпоинты для аналитики
- Функцию #33: Управление System Prompt через UI
- Функцию #34: Исправление багов в загрузке данных
- Функцию #35: Эндпоинт обновления System Prompt
- Функцию #36: Визуализация расходов на OpenAI

---

## 🎯 Быстрый старт

### Для разработчика

1. **Прочитайте PHASE_10_SUMMARY.md** — полный обзор фазы
2. **Изучите конкретные изменения:**
   - Whisper API → `OPENAI_WHISPER_API_COST_LOGGING.md`
   - API эндпоинты → `API_COSTS_DETAILS_ENDPOINT_FIX.md`
   - System Prompt UI → `SYSTEM_PROMPT_UPLOAD_FEATURE.md`

### Для администратора

1. **Откройте админ-панель:** http://mira.uspeshnyy.ru/admin
2. **Проверьте разделы:**
   - Аналитика → Расходы API
   - Конфиг → SYSTEM PROMPT
3. **Прочитайте инструкции:**
   - `SYSTEM_PROMPT_UPLOAD_FEATURE.md` → Как загрузить новый промпт
   - `API_COSTS_DETAILS_ENDPOINT_FIX.md` → Как читать аналитику

---

## 📊 Статистика фазы

**Фаза 10: API Аналитика и Управление**

| Метрика | Значение |
|---------|----------|
| Новых функций | 7 |
| Исправлено багов | 4 |
| Добавлено эндпоинтов | 5 |
| Обновлено модулей | 4 |
| Строк кода | +2000 |
| Часов работы | 15 |
| Файлов изменено | 8 |
| Документов создано | 4 |

---

## 🔧 Технические детали

### Изменённые файлы

```
mira_bot/
├── ai/
│   ├── whisper_client.py                    ← transcribe_bytes() возвращает (text, cost_info)
│   └── prompts/
│       └── backups/                         ← Папка для бэкапов промптов
│
├── bot/
│   └── handlers/
│       └── voice.py                         ← Логирование расходов на транскрибацию
│
├── database/
│   └── repositories/
│       └── api_cost.py                      ← get_costs_list(), get_top_users_by_cost()
│
├── webapp/
│   ├── api/
│   │   ├── main.py                          ← Роутер system_prompt
│   │   └── routes/
│   │       ├── api_costs.py                 ← GET /, GET /stats, GET /by-date, GET /top-users
│   │       └── system_prompt.py             ← POST /update
│   │
│   └── frontend/
│       └── admin.html                       ← UI: API Costs, System Prompt, баг-фиксы
│
└── docs/
    ├── improvements.html                    ← Обновлено: Фаза 10
    └── 30.12.25/
        ├── OPENAI_WHISPER_API_COST_LOGGING.md
        ├── API_COSTS_DETAILS_ENDPOINT_FIX.md
        ├── SYSTEM_PROMPT_UPLOAD_FEATURE.md
        ├── PHASE_10_SUMMARY.md
        └── README.md                        ← Этот файл
```

### Новые API эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/admin/api-costs/` | Детальный список транзакций |
| GET | `/api/admin/api-costs/stats` | Общая статистика расходов |
| GET | `/api/admin/api-costs/by-date` | Расходы по датам для графика |
| GET | `/api/admin/api-costs/top-users` | Топ пользователей по расходам |
| POST | `/api/admin/system-prompt/update` | Обновление системного промпта |

### Схема БД

**Таблица `api_costs`:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Primary key |
| user_id | INTEGER | FK → users.id |
| provider | VARCHAR | claude / openai / yandex_tts |
| operation | VARCHAR | text_generation / speech_to_text / text_to_speech |
| model | VARCHAR | Название модели |
| input_tokens | INTEGER | Входные токены (или NULL для TTS/STT) |
| output_tokens | INTEGER | Выходные токены (или NULL для TTS/STT) |
| total_tokens | INTEGER | Сумма токенов |
| audio_seconds | INTEGER | Длительность аудио (для STT/TTS) |
| cost_usd | DECIMAL | Стоимость в USD |
| created_at | DATETIME | Timestamp создания |

**Пример записи OpenAI:**
```json
{
    "id": 123,
    "user_id": 7,
    "provider": "openai",
    "operation": "speech_to_text",
    "model": "whisper-1",
    "input_tokens": null,
    "output_tokens": null,
    "total_tokens": 0,
    "audio_seconds": 30,
    "cost_usd": 0.003,
    "created_at": "2025-12-30T12:45:00"
}
```

---

## 🚀 Deployment

### 1. Проверка изменений

```bash
# На локальной машине
git status

# Должны быть изменены:
# - ai/whisper_client.py
# - bot/handlers/voice.py
# - database/repositories/api_cost.py
# - webapp/api/main.py
# - webapp/api/routes/api_costs.py
# - webapp/api/routes/system_prompt.py
# - webapp/frontend/admin.html
# - docs/improvements.html
```

### 2. Загрузка на сервер

```bash
# Загрузка изменённых файлов
scp ai/whisper_client.py root@31.44.7.144:/root/mira_bot/ai/
scp bot/handlers/voice.py root@31.44.7.144:/root/mira_bot/bot/handlers/
scp database/repositories/api_cost.py root@31.44.7.144:/root/mira_bot/database/repositories/
scp webapp/api/main.py root@31.44.7.144:/root/mira_bot/webapp/api/
scp webapp/api/routes/api_costs.py root@31.44.7.144:/root/mira_bot/webapp/api/routes/
scp webapp/api/routes/system_prompt.py root@31.44.7.144:/root/mira_bot/webapp/api/routes/
scp webapp/frontend/admin.html root@31.44.7.144:/root/mira_bot/webapp/frontend/
scp docs/improvements.html root@31.44.7.144:/root/mira_bot/docs/
```

### 3. Перезапуск сервисов

```bash
ssh root@31.44.7.144

# Перезапуск webapp
systemctl restart mira-webapp
systemctl status mira-webapp --no-pager

# Перезапуск бота (если нужно)
systemctl restart mirabot
systemctl status mirabot --no-pager
```

### 4. Проверка работы

```bash
# Проверка логов webapp
journalctl -u mira-webapp -n 50 --no-pager

# Проверка логов бота
journalctl -u mirabot -f | grep "Whisper API cost"

# Проверка админ-панели
curl -I http://mira.uspeshnyy.ru/admin
```

---

## ✅ Чек-лист проверки

### Backend

- [ ] Голосовые сообщения логируются в api_costs
- [ ] Лог: "Logged Whisper API cost for user X: $Y (Zs)"
- [ ] В БД есть записи с provider='openai'
- [ ] Эндпоинт `/api/admin/api-costs/` возвращает данные
- [ ] Эндпоинт `/api/admin/system-prompt/update` работает
- [ ] Бэкапы создаются в `ai/prompts/backups/`

### Frontend

- [ ] Секция "Расходы API" загружается без ошибок
- [ ] График показывает линии для всех провайдеров
- [ ] Круговая диаграмма отображает OpenAI
- [ ] Топ пользователей показывает правильные имена
- [ ] Детальная статистика загружается
- [ ] Секция "SYSTEM PROMPT" загружается без ошибок
- [ ] Кнопка "Загрузить новый PROMPT" работает
- [ ] Диалог выбора файла открывается
- [ ] Подтверждение показывает правильный размер файла
- [ ] После загрузки промпт обновляется

### Логирование

- [ ] Логи содержат записи о расходах Whisper
- [ ] Логи содержат записи об обновлении промпта
- [ ] Все ошибки логируются с деталями

---

## 🐛 Troubleshooting

### OpenAI не показывается в аналитике

**Причина:** Нет транзакций в БД

**Решение:**
```bash
# Проверка
sqlite3 mira_bot.db "SELECT COUNT(*) FROM api_costs WHERE provider='openai';"

# Если 0 — отправьте голосовое сообщение боту
```

### Секция System Prompt не загружается

**Причина:** Ошибка JavaScript (двойной .json())

**Решение:**
1. Проверьте версию admin.html на сервере
2. Обновите файл через scp
3. Очистите кэш браузера (Ctrl+Shift+R)

### Ошибка "Failed to create backup"

**Причина:** Нет прав на запись в папку backups

**Решение:**
```bash
ssh root@31.44.7.144
mkdir -p /root/mira_bot/ai/prompts/backups
chmod 755 /root/mira_bot/ai/prompts/backups
```

### 404 на /api-costs/

**Причина:** Роутер не подключён в main.py

**Решение:**
Проверьте что в `webapp/api/main.py` есть:
```python
from webapp.api.routes import api_costs, system_prompt

app.include_router(api_costs.router, prefix="/api/admin", tags=["api-costs"])
app.include_router(system_prompt.router, prefix="/api/admin", tags=["system-prompt"])
```

---

## 📞 Контакты

**Разработчик:** Aleksandr Uspeshnyy
**Telegram:** [@uspeshnyy](https://t.me/uspeshnyy)
**Проект:** Mira Bot
**Версия:** v1.10.0
**Дата:** 30.12.2025

---

## 📄 Лицензия

Все права защищены © 2025 Aleksandr Uspeshnyy
