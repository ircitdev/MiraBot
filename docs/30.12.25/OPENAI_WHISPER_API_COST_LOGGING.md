# Добавлено логирование расходов на OpenAI Whisper API

**Дата:** 30.12.2025
**Задача:** Логировать расходы на транскрибацию голосовых сообщений через OpenAI Whisper API

## 🎯 Зачем это нужно

В боте используется OpenAI Whisper API для транскрибации голосовых сообщений пользователей в текст. До этого момента расходы на Whisper API **НЕ логировались** в базу данных, поэтому:

- OpenAI не показывался в аналитике расходов API
- Не было возможности отслеживать расходы на транскрибацию
- Непонятно сколько стоит обработка голосовых сообщений

**Теперь:** Все расходы на Whisper API логируются в таблицу `api_costs` и отображаются в аналитике.

## ✅ Что было сделано

### 1. Изменён `WhisperClient.transcribe_bytes()`

**Файл:** `ai/whisper_client.py`

**Было:**
```python
async def transcribe_bytes(
    self,
    audio_bytes: bytes,
    file_extension: str = "ogg",
    language: str = "ru",
) -> Optional[str]:
    """Возвращает только текст транскрипции."""
    # ...
    return transcript.strip()
```

**Стало:**
```python
async def transcribe_bytes(
    self,
    audio_bytes: bytes,
    file_extension: str = "ogg",
    language: str = "ru",
    audio_duration_seconds: Optional[int] = None,
) -> Tuple[Optional[str], Dict]:
    """
    Возвращает текст транскрипции И информацию о расходах.

    Returns:
        Tuple[text, cost_info]:
            - text: Транскрибированный текст
            - cost_info: {
                'audio_seconds': int,
                'cost_usd': float,
                'model': str
            }
    """
    cost_info = {
        'audio_seconds': audio_duration_seconds or 0,
        'cost_usd': 0.0,
        'model': self.model
    }

    # Транскрибируем
    result = await self.transcribe(temp_file.name, language)

    # Рассчитываем стоимость
    # Whisper API: $0.006 per minute = $0.0001 per second
    if audio_duration_seconds:
        cost_info['cost_usd'] = audio_duration_seconds * 0.0001

    return result, cost_info
```

**Изменения:**
- Добавлен параметр `audio_duration_seconds` для передачи длительности аудио
- Метод теперь возвращает tuple: `(text, cost_info)` вместо просто `text`
- Расчёт стоимости: $0.0001 за секунду ($0.006 за минуту)

### 2. Обновлён обработчик голосовых сообщений

**Файл:** `bot/handlers/voice.py`

**Добавлено:**

#### 2.1. Импорт `ApiCostRepository`
```python
from database.repositories.api_cost import ApiCostRepository

# Инициализируем сервисы
api_cost_repo = ApiCostRepository()
```

#### 2.2. Обновлён вызов транскрибации
```python
# Было:
transcribed_text = await whisper_client.transcribe_bytes(
    bytes(voice_bytes),
    file_extension="ogg",
    language="ru",
)

# Стало:
transcribed_text, whisper_cost_info = await whisper_client.transcribe_bytes(
    bytes(voice_bytes),
    file_extension="ogg",
    language="ru",
    audio_duration_seconds=voice.duration,  # Передаём длительность
)
```

#### 2.3. Добавлено сохранение расходов в БД
```python
# Сохраняем расходы на транскрибацию
if whisper_cost_info['cost_usd'] > 0:
    try:
        await api_cost_repo.create(
            user_id=user.id,
            provider='openai',
            operation='speech_to_text',
            cost_usd=whisper_cost_info['cost_usd'],
            audio_seconds=whisper_cost_info['audio_seconds'],
            model=whisper_cost_info['model'],
        )
        logger.info(
            f"Logged Whisper API cost for user {user.id}: "
            f"${whisper_cost_info['cost_usd']:.6f} "
            f"({whisper_cost_info['audio_seconds']}s)"
        )
    except Exception as e:
        logger.error(f"Failed to log Whisper API cost: {e}")
```

**Что сохраняется:**
- `provider`: `'openai'`
- `operation`: `'speech_to_text'`
- `cost_usd`: Стоимость в USD (длительность × $0.0001)
- `audio_seconds`: Длительность аудио в секундах
- `model`: Модель Whisper (обычно `whisper-1`)
- `user_id`: ID пользователя

## 📊 Как это работает

### Пример: Голосовое сообщение 30 секунд

1. Пользователь отправляет голосовое сообщение длительностью 30 секунд
2. Бот транскрибирует через Whisper API
3. Рассчитывается стоимость: `30 * 0.0001 = $0.003`
4. Сохраняется запись в `api_costs`:
   ```json
   {
       "user_id": 7,
       "provider": "openai",
       "operation": "speech_to_text",
       "cost_usd": 0.003,
       "audio_seconds": 30,
       "model": "whisper-1"
   }
   ```
5. В логах: `Logged Whisper API cost for user 7: $0.003000 (30s)`

### Цены OpenAI Whisper API

**Официальная цена:** $0.006 per minute

**Наш расчёт:**
- $0.006 / 60 секунд = $0.0001 за секунду
- 10 секунд = $0.001
- 30 секунд = $0.003
- 1 минута = $0.006
- 5 минут = $0.030

## 📈 Где теперь отображаются расходы

После этого изменения расходы на OpenAI Whisper будут показаны в админ-панели:

### 1. Раздел "Аналитика" → "Расходы API"

**Карточки статистики:**
- В карточке "Всего потрачено" будет учитываться OpenAI
- Появится отдельная карточка или раздел для OpenAI (если добавить в UI)

**Графики:**
- **Динамика расходов:** Линия для OpenAI (синий цвет #3B82F6)
- **По провайдерам:** Сектор для OpenAI на круговой диаграмме

**Топ-10 пользователей:**
```
# | Пользователь | Telegram ID | Провайдер | Токены | Стоимость
1 | Елена        | 1926322383  | OpenAI    | 0      | $0.0120
```

**Детальная статистика:**
```
Дата          | Пользователь | Провайдер | Модель    | Секунды аудио | Стоимость
30.12, 12:45  | Елена        | OpenAI    | whisper-1 | 45s          | $0.0045
30.12, 12:40  | Лиза         | OpenAI    | whisper-1 | 30s          | $0.0030
```

### 2. SQL запросы

**Общие расходы по провайдерам:**
```sql
SELECT provider, COUNT(*), SUM(cost_usd)
FROM api_costs
GROUP BY provider;

-- Результат:
-- claude | 86 | 6.206256
-- openai | 15 | 0.045000  ← Появится после использования
```

**Расходы на Whisper за сегодня:**
```sql
SELECT
    u.display_name,
    COUNT(*) as voice_count,
    SUM(ac.audio_seconds) as total_seconds,
    SUM(ac.cost_usd) as total_cost
FROM api_costs ac
JOIN users u ON u.id = ac.user_id
WHERE ac.provider = 'openai'
  AND DATE(ac.created_at) = CURRENT_DATE
GROUP BY u.id, u.display_name
ORDER BY total_cost DESC;
```

## 🔍 Проверка

### 1. Отправьте голосовое сообщение боту

Отправьте любое голосовое сообщение в бота (например, 10-20 секунд).

### 2. Проверьте логи бота

```bash
ssh root@31.44.7.144
journalctl -u mirabot -f | grep "Whisper API cost"
```

**Ожидаемый лог:**
```
Logged Whisper API cost for user 7: $0.001500 (15s)
```

### 3. Проверьте базу данных

```bash
ssh root@31.44.7.144
cd /root/mira_bot
sqlite3 mira_bot.db "
SELECT
    ac.created_at,
    u.display_name,
    ac.provider,
    ac.audio_seconds,
    ac.cost_usd,
    ac.model
FROM api_costs ac
JOIN users u ON u.id = ac.user_id
WHERE ac.provider = 'openai'
ORDER BY ac.created_at DESC
LIMIT 5;
"
```

**Ожидаемый результат:**
```
2025-12-30 12:45:00|Елена|openai|45|0.0045|whisper-1
2025-12-30 12:40:15|Лиза|openai|30|0.003|whisper-1
```

### 4. Проверьте админ-панель

1. Откройте http://mira.uspeshnyy.ru/admin
2. Перейдите в "Аналитика" → "Расходы API"
3. Обновите страницу (Ctrl+Shift+R)
4. Должны появиться данные по OpenAI:
   - На графике "Динамика расходов" появится синяя линия OpenAI
   - На круговой диаграмме появится сектор OpenAI
   - В таблице "Детальная статистика" будут записи с провайдером OpenAI

## 📋 Файлы изменений

**1. ai/whisper_client.py**
- Строка 9: Добавлен импорт `Tuple, Dict`
- Строки 55-116: Изменён метод `transcribe_bytes()` - теперь возвращает tuple с cost_info

**2. bot/handlers/voice.py**
- Строка 16: Добавлен импорт `ApiCostRepository`
- Строка 27: Инициализирован `api_cost_repo`
- Строки 109-114: Обновлён вызов `transcribe_bytes()` с передачей длительности
- Строки 116-133: Добавлено логирование расходов в БД

## 🎉 Итог

✅ **Расходы на OpenAI Whisper API теперь логируются!**

**Что работает:**
- Каждая транскрибация голосового сообщения логируется в `api_costs`
- Расчёт стоимости: $0.0001 за секунду аудио
- Данные сохраняются с provider='openai', operation='speech_to_text'
- Расходы отображаются в аналитике админ-панели

**Что показывается:**
- Общие расходы на OpenAI в карточках статистики
- График динамики расходов на OpenAI (синий)
- Круговая диаграмма: доля OpenAI среди всех провайдеров
- Топ пользователей по расходам на OpenAI
- Детальная статистика каждой транскрибации

**Следующие шаги:**
- Отправьте несколько голосовых сообщений для тестирования
- Проверьте что данные появились в админ-панели
- Мониторьте расходы на транскрибацию
