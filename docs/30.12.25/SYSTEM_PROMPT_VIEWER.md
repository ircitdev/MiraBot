# Добавлен просмотр System Prompt в админ-панели

**Дата:** 30.12.2025

**Что добавлено:** Новый подраздел "SYSTEM PROMPT" в меню Контекст для просмотра системного промпта Миры, его истории версий и возможности скачать.

## 🎯 Функциональность

### 1. Кнопка в меню Контекст

Добавлена третья кнопка в подменю Контекст:

- 😊 **Эмоции** - справочник эмоций
- 👤 **Легенда** - информация о персонаже Мира
- 📝 **SYSTEM PROMPT** - системный промпт (новое!)

### 2. Текущая версия

**Отображается:**
- Версия промпта (v1.0)
- Дата последнего изменения
- Размер файла в символах
- Полный текст промпта

**Кнопки:**
- ✅ **Обновить** - перезагрузить промпт с сервера
- 📥 **Скачать** - скачать промпт как .txt файл
- 📋 **Копировать** - скопировать весь текст в буфер обмена

### 3. История версий

Таблица с историей всех версий системного промпта:

| Версия | Дата | Размер | Описание | Действия |
|--------|------|--------|----------|----------|
| v1.1 | 29.12.25, 10:00 | 15,234 символов | Updated personality traits | 👁️ Просмотр, 📥 |
| v1.0 | 01.12.25, 10:00 | 14,892 символов | Initial version | 👁️ Просмотр, 📥 |

**Кнопки для каждой версии:**
- 👁️ **Просмотр** - открыть версию в модальном окне
- 📥 **Скачать** - скачать эту версию как .txt файл

## 📁 Откуда берётся промпт

Системный промпт читается из файла:
```
/root/mira_bot/ai/prompts/system_prompt.py
```

Это файл на 1072 строки, который содержит всю логику построения системного промпта Миры.

## 🔧 Технические детали

### Frontend (admin.html)

**Добавлено:**

1. **Кнопка в подменю** (строки 3728-3731):
```html
<button class="sub-nav-item" data-section="system-prompt" onclick="switchContextSection('system-prompt')">
    <span class="material-icons">code</span>
    SYSTEM PROMPT
</button>
```

2. **HTML секция** (строки 5316-5376):
```html
<div id="context-system-prompt" class="context-section">
    <!-- Текущая версия с кнопками скачать/копировать -->
    <!-- История версий -->
</div>
```

3. **JavaScript функции** (строки 6991-7169):
- `loadSystemPrompt()` - загрузить текущий промпт
- `loadSystemPromptHistory()` - загрузить историю версий
- `renderSystemPromptHistory()` - отобразить таблицу версий
- `viewSystemPromptVersion()` - показать версию в модальном окне
- `downloadSystemPrompt()` - скачать промпт как файл
- `copyToClipboard()` - копировать текст в буфер обмена

### Backend API

**Новый файл:** `webapp/api/routes/system_prompt.py`

**Эндпоинты:**

#### 1. GET `/api/admin/system-prompt/current`
```python
@router.get("/current")
async def get_current_system_prompt(admin_data=Depends(require_admin_role))
```

Возвращает:
```json
{
  "version": "1.0",
  "content": "...полный текст промпта...",
  "updated_at": "2025-12-30T09:40:00"
}
```

#### 2. GET `/api/admin/system-prompt/history`
```python
@router.get("/history")
async def get_system_prompt_history(admin_data=Depends(require_admin_role))
```

Возвращает:
```json
{
  "history": []
}
```

**Примечание:** На данный момент возвращает пустой массив. История версий будет реализована позже с сохранением в базу данных.

#### 3. GET `/api/admin/system-prompt/version/{version}`
```python
@router.get("/version/{version}")
async def get_system_prompt_version(version: str, admin_data=Depends(require_admin_role))
```

Возвращает:
```json
{
  "version": "1.0",
  "content": "...полный текст промпта...",
  "created_at": "2025-12-30T09:40:00",
  "description": "System prompt version 1.0"
}
```

### Регистрация роутера

**Файл:** `webapp/api/main.py` (строка 12, 41)

```python
from webapp.api.routes import ..., system_prompt

app.include_router(system_prompt.router, prefix="/api/admin", tags=["system-prompt"])
```

## ✅ Результат

После обновления страницы (Ctrl + Shift + R):

1. ✅ В меню Контекст появилась кнопка "SYSTEM PROMPT"
2. ✅ При клике открывается страница с текущим промптом
3. ✅ Промпт читается из файла `system_prompt.py` на сервере
4. ✅ Можно скачать промпт как .txt файл
5. ✅ Можно скопировать весь текст в буфер обмена
6. ✅ История версий показывает "Нет истории версий" (пока не реализовано)

## 📋 Что нужно сделать

### 1. Жёсткая перезагрузка страницы

**ОБЯЗАТЕЛЬНО!** Браузер кеширует старый файл.

```
Ctrl + Shift + R (или Ctrl + F5)
```

### 2. Открыть раздел System Prompt

1. Откройте админ-панель
2. Перейдите в **Контекст**
3. Нажмите кнопку **SYSTEM PROMPT**
4. Дождитесь загрузки (несколько секунд, файл большой - 1072 строки)

### 3. Проверить функциональность

- ✅ Текст промпта отображается
- ✅ Дата и размер показываются корректно
- ✅ Кнопка "Скачать" сохраняет файл `system_prompt_1.0.txt`
- ✅ Кнопка "Копировать" копирует текст в буфер обмена
- ✅ История версий показывает "Нет истории версий"

## 🔮 Будущие улучшения

### Версионирование (TODO)

В будущем планируется:

1. **Сохранение версий в БД**
   - Таблица `system_prompt_versions`
   - Поля: `id`, `version`, `content`, `description`, `created_at`, `created_by`

2. **Автоматическое сохранение при изменении**
   - При изменении файла `system_prompt.py`
   - Git хуки для автоматического версионирования

3. **Сравнение версий**
   - Diff между двумя версиями
   - Подсветка изменений

4. **Откат к предыдущей версии**
   - Восстановление любой версии из истории

5. **Комментарии к версиям**
   - Описание что изменилось
   - Кто и когда внёс изменения

## 📄 Файлы изменений

**Frontend:**
- `webapp/frontend/admin.html`
  - Строки 3728-3731: кнопка в меню
  - Строки 5316-5376: HTML секция
  - Строки 6580-6583: обработчик переключения
  - Строки 6991-7169: JavaScript функции

**Backend:**
- `webapp/api/routes/system_prompt.py` (новый файл)
- `webapp/api/main.py` (строки 12, 41)

## 🎉 Итог

✅ **Добавлен просмотр системного промпта в админ-панели!**

Теперь администраторы могут:
- Просматривать текущий системный промпт Миры
- Видеть когда он был последний раз изменён
- Скачивать промпт как .txt файл
- Копировать текст в буфер обмена
- (В будущем) Просматривать историю всех версий

**Что показывается:**
- Полный текст из файла `ai/prompts/system_prompt.py` (1072 строки)
- Дата последнего изменения файла
- Размер файла в символах
- Версия v1.0 (пока статическая)

Все данные загружаются через защищённое API с проверкой прав администратора!
