# WebApp Monitoring System

**Дата создания:** 15 декабря 2025
**Статус:** ✅ Активно

---

## 📋 Обзор

Система автоматического мониторинга и перезапуска WebApp для обеспечения бесперебойной работы мини-приложения Telegram бота Мира.

---

## 🛠️ Компоненты

### 1. Systemd Service: `mirabot-webapp.service`

**Файл:** `/etc/systemd/system/mirabot-webapp.service`

**Функции:**
- Автоматический запуск WebApp при загрузке сервера
- Автоматический перезапуск при падении (RestartSec=10s)
- Watchdog timeout: 60 секунд
- Ограничения ресурсов: 512MB RAM, 50% CPU

**Управление:**
```bash
# Статус
systemctl status mirabot-webapp

# Перезапуск
systemctl restart mirabot-webapp

# Логи
journalctl -u mirabot-webapp -f

# Логи приложения
tail -f /var/log/mira_webapp.log
```

### 2. Monitor Script: `monitor_webapp.sh`

**Файл:** `/root/mira_bot/scripts/monitor_webapp.sh`

**Функции:**
- Проверка HTTP доступности (http://127.0.0.1:8081/)
- Проверка статуса systemd сервиса
- 3 retry попытки с задержкой 5 секунд
- Автоматический перезапуск при проблемах
- Логирование всех событий

**Логи:** `/var/log/mira_webapp_monitor.log`

**Ручной запуск:**
```bash
/root/mira_bot/scripts/monitor_webapp.sh
```

### 3. Systemd Timer: `mirabot-webapp-monitor.timer`

**Файл:** `/etc/systemd/system/mirabot-webapp-monitor.timer`

**Расписание:**
- После загрузки: через 2 минуты
- Периодичность: каждые 5 минут

**Управление:**
```bash
# Статус
systemctl status mirabot-webapp-monitor.timer

# Посмотреть расписание
systemctl list-timers | grep mira

# Ручной запуск проверки
systemctl start mirabot-webapp-monitor.service

# Логи последней проверки
journalctl -u mirabot-webapp-monitor.service -n 50
```

---

## 📊 Алгоритм работы

```
┌─────────────────────────────────────┐
│  Timer: каждые 5 минут              │
└──────────────┬──────────────────────┘
               ▼
┌─────────────────────────────────────┐
│  Проверка: процесс запущен?         │
└──────────────┬──────────────────────┘
               │
         ┌─────┴─────┐
         │    Да     │
         └─────┬─────┘
               ▼
┌─────────────────────────────────────┐
│  Проверка: HTTP 200 от WebApp?      │
└──────────────┬──────────────────────┘
               │
         ┌─────┴─────┐
    ┌────┤    Нет    ├────┐
    │    └───────────┘    │
    │                     │
    ▼                     ▼
┌────────┐         ┌─────────────┐
│ Retry  │         │ 3 попытки?  │
│(3 раза)│         └─────┬───────┘
└────┬───┘               │
     │              ┌────┴────┐
     │              │   Да    │
     └──────────────┴────┬────┘
                         ▼
              ┌──────────────────────┐
              │ systemctl restart    │
              │ mirabot-webapp       │
              └──────────┬───────────┘
                         ▼
              ┌──────────────────────┐
              │ Финальная проверка   │
              └──────────────────────┘
```

---

## 🔍 Мониторинг и диагностика

### Проверка статуса всей системы
```bash
# Все сервисы
systemctl status mirabot mirabot-webapp --no-pager

# Timers
systemctl list-timers | grep mira

# Последние логи WebApp
tail -50 /var/log/mira_webapp.log

# Логи мониторинга
tail -50 /var/log/mira_webapp_monitor.log

# Сетевые соединения
netstat -tlnp | grep 8081
```

### Типичные проблемы и решения

#### Проблема: WebApp не отвечает
```bash
# Проверить процесс
ps aux | grep webapp

# Проверить порт
netstat -tlnp | grep 8081

# Перезапуск
systemctl restart mirabot-webapp

# Если не помогает - проверить логи
journalctl -u mirabot-webapp -n 100
```

#### Проблема: Мониторинг не работает
```bash
# Проверить timer
systemctl status mirabot-webapp-monitor.timer

# Проверить права на скрипт
ls -la /root/mira_bot/scripts/monitor_webapp.sh

# Ручной запуск для диагностики
bash -x /root/mira_bot/scripts/monitor_webapp.sh
```

#### Проблема: 504 Gateway Timeout
```bash
# Проверить nginx конфигурацию
nginx -t

# Проверить upstream
curl -v http://127.0.0.1:8081/

# Перезапустить всё
systemctl restart mirabot-webapp nginx
```

---

## 📈 Метрики и алерты

### Что мониторится:
- ✅ HTTP доступность (код 200)
- ✅ Статус systemd сервиса
- ✅ Таймауты соединения (5 секунд)
- ✅ Таймауты ответа (10 секунд)

### Логирование:
- **Приложение:** `/var/log/mira_webapp.log`
- **Мониторинг:** `/var/log/mira_webapp_monitor.log`
- **Systemd:** `journalctl -u mirabot-webapp`

### Критерии перезапуска:
- HTTP код != 200 (3 попытки)
- Таймаут соединения
- Процесс не запущен
- Watchdog timeout (60 секунд без ответа)

---

## 🚀 Установка на новый сервер

```bash
# 1. Скопировать файлы
scp scripts/monitor_webapp.sh root@SERVER:/root/mira_bot/scripts/
chmod +x /root/mira_bot/scripts/monitor_webapp.sh

# 2. Создать systemd unit files
# (см. секцию Компоненты выше)

# 3. Активировать
systemctl daemon-reload
systemctl enable mirabot-webapp
systemctl enable mirabot-webapp-monitor.timer
systemctl start mirabot-webapp
systemctl start mirabot-webapp-monitor.timer

# 4. Проверить
systemctl status mirabot-webapp
systemctl list-timers | grep mira
```

---

## 📝 История изменений

### 15.12.2025 - Начальная версия
- Создан systemd service с watchdog
- Добавлен скрипт мониторинга
- Настроен timer на 5-минутную проверку
- Добавлено логирование

---

## 🔧 Настройка

### Изменить частоту проверок
Редактировать `/etc/systemd/system/mirabot-webapp-monitor.timer`:
```ini
[Timer]
OnUnitActiveSec=3min  # Было: 5min
```

Затем:
```bash
systemctl daemon-reload
systemctl restart mirabot-webapp-monitor.timer
```

### Изменить timeout'ы
Редактировать `/root/mira_bot/scripts/monitor_webapp.sh`:
```bash
MAX_RETRIES=5      # Было: 3
RETRY_DELAY=10     # Было: 5
```

### Изменить watchdog timeout
Редактировать `/etc/systemd/system/mirabot-webapp.service`:
```ini
[Service]
WatchdogSec=120    # Было: 60
```

---

## 📞 Контакты

**Разработчик:** Claude Code
**Дата:** 15.12.2025
**Версия:** 1.0

---

**Статус:** ✅ Система мониторинга активна и работает
