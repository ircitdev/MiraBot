# Техническое задание: Telegram-бот технической поддержки MiraDrugSupport

**Дата создания:** 03.01.2026
**Версия:** 1.0
**Статус:** Draft

---

## 📋 Общие сведения

### Основная информация
- **Название бота:** MiraDrugSupport
- **Username:** `@MiraDrugSupport_bot`
- **Целевая супергруппа:** `https://t.me/MiraBotEvents/`
  - **Топик для поддержки:** #2 (Support)
  - **Топик для отзывов:** #4 (Reviews)
  - **Требования:** Включить функцию "Темы/Topics" в настройках группы

### Цели проекта
1. **Основная цель:** Организация эффективной службы поддержки через Telegram с централизованным управлением
2. **Техническая цель:** Автоматическая синхронизация переписки между пользователями и администраторами
3. **Управленческая цель:** Единый интерфейс для просмотра истории обращений и отзывов в Web-админке

### Архитектурные принципы
- **Минимум дублирования:** Один топик = один пользователь (lifetime)
- **Прозрачность:** Вся история доступна в админке
- **Асинхронность:** Администраторы отвечают в удобное время
- **Масштабируемость:** Поддержка неограниченного числа пользователей

---

## 🎯 Функциональные требования

### 1. Telegram-бот (Пользовательская часть)

#### 1.1. Команда /start
**Сценарий для нового пользователя:**
1. Пользователь отправляет `/start` боту `@MiraDrugSupport_bot`
2. Бот проверяет наличие пользователя в БД по `telegram_id`
3. **Если пользователя НЕТ в БД:**
   - Бот создаёт новый **Forum Topic** в супергруппе `MiraBotEvents` (топик #2 - Support)
   - **Название топика:** `{first_name} {last_name}` (или только `{first_name}` если нет фамилии)
   - Бот сохраняет в БД:
     - `user.telegram_id` = ID пользователя
     - `user.first_name` = Имя
     - `user.last_name` = Фамилия (если есть)
     - `user.username` = @username (если есть)
     - `user.photo_url` = Ссылка на аватар или file_id
     - `user.topic_id` = ID созданного топика
     - `user.created_at` = Текущее время
   - Бот отправляет **первое сообщение в топик** (карточку пользователя):

   ```
   🆕 Новый пользователь

   👤 Имя: Александр Успешный
   🆔 ID: 65876198
   🔗 Профиль: tg://user?id=65876198
   📱 Username: @uspeshnyy

   ⏰ Первое обращение: 03.01.2026 15:30
   ```

4. **Если пользователь УЖЕ ЕСТЬ в БД:**
   - Бот находит существующий `topic_id`
   - Новый топик НЕ создаётся
   - Все новые сообщения идут в существующий топик

5. Бот отправляет приветственное сообщение пользователю:
   ```
   Привет! 👋

   Я бот поддержки Миры — твоего друга для эмоциональной поддержки.

   Опиши свою проблему или вопрос — наша команда ответит в ближайшее время.

   💡 Все сообщения (текст, фото, видео, голосовые) будут переданы специалистам.
   ```

#### 1.2. Пересылка сообщений: Пользователь → Поддержка
**Поддерживаемые типы сообщений:**
- ✅ Текст
- ✅ Фото (с подписью и без)
- ✅ Видео (с подписью и без)
- ✅ Голосовые сообщения
- ✅ Видеосообщения (кружочки)
- ✅ Документы
- ✅ Стикеры (опционально, можно отключить)

**Алгоритм пересылки:**
1. Пользователь отправляет сообщение боту
2. Бот определяет `topic_id` пользователя из БД
3. Бот **пересылает** (forward) или **дублирует** сообщение в топик пользователя
4. Бот сохраняет запись в таблицу `support_messages`:
   - `user_id` = ID пользователя
   - `sender_type` = `"user"`
   - `message_text` = Текст сообщения
   - `media_type` = `"text"` / `"photo"` / `"video"` / `"voice"` / `"document"`
   - `media_file_id` = file_id из Telegram (для медиа)
   - `telegram_message_id` = ID сообщения в Telegram
   - `created_at` = Timestamp
5. (Опционально) Бот отправляет подтверждение пользователю:
   ```
   ✅ Сообщение получено. Ожидайте ответа от специалиста.
   ```

#### 1.3. Пересылка сообщений: Поддержка → Пользователь
**Алгоритм:**
1. Администратор отвечает **в топике пользователя** (reply или просто пишет в топик)
2. Бот слушает все сообщения в супергруппе `MiraBotEvents`
3. Бот определяет:
   - **Топик:** По `message_thread_id` (ID топика)
   - **Пользователь:** По связке `topic_id` → `user_id` из БД
4. Бот отправляет сообщение пользователю **от имени бота**
5. Бот сохраняет запись в `support_messages`:
   - `sender_type` = `"admin"`
   - Остальные поля аналогично

**Важно:**
- Администратор может отправлять текст, фото, видео, документы
- Голосовые сообщения от админа НЕ поддерживаются (ограничение Telegram Bot API)
- Бот НЕ пересылает служебные сообщения (например, "User joined the group")

#### 1.4. Дополнительные команды
- `/help` — Справка по использованию бота
- `/cancel` — Отменить текущую операцию (если есть FSM)

---

### 2. База данных

#### 2.1. Новая таблица: `support_users`
Отдельная таблица для пользователей поддержки (не путать с основной `users`).

```python
class SupportUser(Base):
    __tablename__ = "support_users"

    id = Column(Integer, primary_key=True, autoincrement=True)
    telegram_id = Column(BigInteger, unique=True, nullable=False, index=True)
    first_name = Column(String(255))
    last_name = Column(String(255), nullable=True)
    username = Column(String(255), nullable=True)
    photo_url = Column(String(500), nullable=True)  # URL аватара или file_id

    topic_id = Column(Integer, nullable=False, index=True)  # ID топика в супергруппе

    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    # Связи
    messages = relationship("SupportMessage", back_populates="user", cascade="all, delete-orphan")
    reviews = relationship("SupportReview", back_populates="user", cascade="all, delete-orphan")
```

**Индексы:**
- `telegram_id` (unique)
- `topic_id` (для быстрого поиска по топику)

#### 2.2. Новая таблица: `support_messages`
История всех сообщений в поддержке.

```python
class SupportMessage(Base):
    __tablename__ = "support_messages"

    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(Integer, ForeignKey("support_users.id"), nullable=False, index=True)

    sender_type = Column(SQLEnum("user", "admin", name="sender_type_enum"), nullable=False)

    message_text = Column(Text, nullable=True)  # Null для медиа без подписи
    media_type = Column(SQLEnum("text", "photo", "video", "voice", "video_note", "document", "sticker", name="media_type_enum"), default="text")
    media_file_id = Column(String(500), nullable=True)  # file_id из Telegram

    telegram_message_id = Column(BigInteger, nullable=True)  # ID сообщения в Telegram (для редактирования/удаления)

    is_read = Column(Boolean, default=False)  # Прочитано пользователем/админом (опционально)

    created_at = Column(DateTime, default=datetime.utcnow, nullable=False, index=True)

    # Связи
    user = relationship("SupportUser", back_populates="messages")
```

**Индексы:**
- `user_id` (для быстрого поиска по пользователю)
- `created_at` (для сортировки по времени)
- Составной индекс: `(user_id, created_at)` для пагинации

#### 2.3. Новая таблица: `support_reviews`
Отзывы пользователей о Мире (из топика #4).

```python
class SupportReview(Base):
    __tablename__ = "support_reviews"

    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(Integer, ForeignKey("support_users.id"), nullable=True, index=True)  # Nullable, если отзыв анонимный

    # Данные из формы отзыва
    username = Column(String(255), nullable=True)  # @username из Telegram
    age = Column(Integer, nullable=True)
    about_self = Column(Text, nullable=True)  # "О себе"
    review_text = Column(Text, nullable=False)  # Текст отзыва
    permission_to_publish = Column(Boolean, default=False)  # Разрешение на публикацию

    # Метаданные
    telegram_message_id = Column(BigInteger, nullable=True)  # ID сообщения в топике #4
    created_at = Column(DateTime, default=datetime.utcnow, nullable=False, index=True)

    # Связи
    user = relationship("SupportUser", back_populates="reviews")
```

**Структура сообщения в топике #4:**
```
👤 От: @uspeshnyy
🎂 Возраст: 35
ℹ️ О себе: Предприниматель, отец двоих детей
💬 Отзыв: Мира помогла мне разобраться в сложной ситуации...
📢 Разрешение: Да
```

---

### 3. Web-админка (Расширение существующей)

#### 3.1. Новый раздел: "Поддержка"
Добавить в главное меню админки новый пункт `"Поддержка"` с двумя подразделами:
- **"Вопросы"** — История обращений
- **"Отзывы"** — Отзывы пользователей

#### 3.2. Подраздел "Вопросы"

**UI/UX:**
- **Формат:** Список-аккордеон (Accordion List)
- **Сортировка:** По дате последнего сообщения (новые сверху)
- **Пагинация:** 20 элементов на страницу

**Элемент списка (свёрнутый):**
```
┌─────────────────────────────────────────────────────────────────┐
│ 👤 [Аватар] Александр Успешный                                  │
│             14 сообщений · Последнее: 03.01.2026 14:30          │
└─────────────────────────────────────────────────────────────────┘
```

**Структура данных:**
- Аватарка (круглая, 40x40px)
- `{first_name} {last_name}`
- Кол-во сообщений: `{total_messages} сообщений`
- Дата последнего сообщения: `{last_message_date}`

**Элемент списка (развёрнутый):**
```
┌─────────────────────────────────────────────────────────────────┐
│ [Хедер]                                                          │
│ 👤 [Аватар] Александр Успешный                                  │
│             @uspeshnyy · ID: 65876198                           │
│             [Открыть в Telegram] [Открыть топик]                │
├─────────────────────────────────────────────────────────────────┤
│ [Тело чата]                                                      │
│                                                                  │
│ ┌─ Пользователь ──────────────────────────────────────┐         │
│ │ Здравствуйте! У меня проблема с ботом...            │         │
│ │ 03.01.2026 10:15                                     │         │
│ └──────────────────────────────────────────────────────┘         │
│                                                                  │
│         ┌─ Администратор ─────────────────────────────┐         │
│         │ Добрый день! Опишите подробнее проблему.   │         │
│         │ 03.01.2026 11:20                            │         │
│         └─────────────────────────────────────────────┘         │
│                                                                  │
│ ┌─ Пользователь ──────────────────────────────────────┐         │
│ │ Бот не отвечает на мои сообщения...                 │         │
│ │ 03.01.2026 11:25                                     │         │
│ └──────────────────────────────────────────────────────┘         │
│                                                                  │
│ [Загрузить ещё (10 сообщений)]                                  │
└─────────────────────────────────────────────────────────────────┘
```

**Стилизация сообщений:**
- **Сообщения пользователя:**
  - Выравнивание: слева
  - Фон: `#F0F0F0` (светло-серый)
  - Цвет текста: `#333333`
  - Border-radius: `12px`
  - Padding: `12px`
  - Max-width: `70%`

- **Сообщения администратора:**
  - Выравнивание: справа
  - Фон: `#007AFF` (синий) или `#34C759` (зелёный, Material Design)
  - Цвет текста: `#FFFFFF`
  - Border-radius: `12px`
  - Padding: `12px`
  - Max-width: `70%`

**Отображение медиа:**
- Фото: превью 200x200px, клик — полный размер
- Видео: превью с кнопкой Play
- Голосовые: аудиоплеер с длительностью
- Документы: иконка + название файла + кнопка "Скачать"

**API Endpoint:**
```
GET /api/support/questions
Response:
{
  "total": 42,
  "page": 1,
  "per_page": 20,
  "questions": [
    {
      "user_id": 1,
      "telegram_id": 65876198,
      "first_name": "Александр",
      "last_name": "Успешный",
      "username": "uspeshnyy",
      "avatar_url": "https://...",
      "topic_id": 123,
      "total_messages": 14,
      "last_message_date": "2026-01-03T14:30:00Z",
      "last_message_text": "Спасибо за помощь!"
    },
    ...
  ]
}

GET /api/support/questions/{user_id}/messages?page=1&limit=50
Response:
{
  "total": 14,
  "page": 1,
  "messages": [
    {
      "id": 1,
      "sender_type": "user",
      "message_text": "Здравствуйте! У меня проблема...",
      "media_type": "text",
      "media_file_id": null,
      "created_at": "2026-01-03T10:15:00Z"
    },
    {
      "id": 2,
      "sender_type": "admin",
      "message_text": "Добрый день!",
      "media_type": "text",
      "created_at": "2026-01-03T11:20:00Z"
    },
    ...
  ]
}
```

#### 3.3. Подраздел "Отзывы"

**UI/UX:**
- **Формат:** Карточки (Card Grid) или таблица
- **Сортировка:** По дате (новые сверху)
- **Фильтрация:**
  - По разрешению на публикацию (Да/Нет)
  - По дате (за последний месяц, 3 месяца, всё время)

**Карточка отзыва:**
```
┌───────────────────────────────────────────────────────┐
│ 👤 От: @uspeshnyy                                     │
│ 🎂 Возраст: 35                                        │
│ ℹ️ О себе: Предприниматель, отец двоих детей         │
│                                                       │
│ 💬 Отзыв:                                             │
│ "Мира помогла мне разобраться в сложной ситуации...  │
│  Очень благодарен за поддержку!"                     │
│                                                       │
│ 📢 Разрешение на публикацию: ✅ Да                    │
│ 📅 Дата: 03.01.2026 15:45                            │
│                                                       │
│ [Открыть в Telegram] [Экспорт]                       │
└───────────────────────────────────────────────────────┘
```

**API Endpoint:**
```
GET /api/support/reviews?page=1&permission=true
Response:
{
  "total": 28,
  "page": 1,
  "per_page": 20,
  "reviews": [
    {
      "id": 1,
      "username": "uspeshnyy",
      "age": 35,
      "about_self": "Предприниматель, отец двоих детей",
      "review_text": "Мира помогла мне...",
      "permission_to_publish": true,
      "created_at": "2026-01-03T15:45:00Z",
      "telegram_message_id": 12345
    },
    ...
  ]
}
```

**Дополнительные функции:**
- **Экспорт отзывов:** CSV/JSON для использования на лендинге
- **Поиск:** По тексту отзыва, имени пользователя

#### 3.4. Раздел "Конфиг" (Настройки)

Добавить в существующий раздел "Конфиг" новую секцию **"Поддержка"**:

**Настройки:**
1. **Support Bot Token**
   - Тип: `string` (секрет)
   - Описание: "Токен бота @MiraDrugSupport_bot"
   - Переменная окружения: `SUPPORT_BOT_TOKEN`

2. **Support Group ID**
   - Тип: `integer`
   - Описание: "ID супергруппы MiraBotEvents (получить через бота)"
   - Переменная окружения: `SUPPORT_GROUP_ID`

3. **Support Topic ID**
   - Тип: `integer`
   - Описание: "ID топика для поддержки (по умолчанию: 2)"
   - Переменная окружения: `SUPPORT_TOPIC_ID`
   - Default: `2`

4. **Reviews Topic ID**
   - Тип: `integer`
   - Описание: "ID топика для отзывов (по умолчанию: 4)"
   - Переменная окружения: `REVIEWS_TOPIC_ID`
   - Default: `4`

5. **Auto-reply message**
   - Тип: `text`
   - Описание: "Автоматическое сообщение после получения запроса"
   - Default: `"✅ Сообщение получено. Ожидайте ответа от специалиста."`

6. **Support Enabled**
   - Тип: `boolean`
   - Описание: "Включить/выключить бота поддержки"
   - Default: `true`

**UI для настроек:**
```
┌─────────────────────────────────────────────────────┐
│ Поддержка                                           │
├─────────────────────────────────────────────────────┤
│                                                     │
│ Support Bot Token:                                  │
│ [********************]                              │
│                                                     │
│ Support Group ID:                                   │
│ [-1001234567890]                                    │
│ ℹ️ Получить через команду /id в группе             │
│                                                     │
│ Support Topic ID:                                   │
│ [2]                                                 │
│                                                     │
│ Reviews Topic ID:                                   │
│ [4]                                                 │
│                                                     │
│ Auto-reply message:                                 │
│ [✅ Сообщение получено. Ожидайте ответа...]        │
│                                                     │
│ Support Enabled:                                    │
│ [✓] Включено                                        │
│                                                     │
│ [Сохранить настройки]                              │
└─────────────────────────────────────────────────────┘
```

---

## 🔧 Технические требования

### 4.1. Архитектура бота

**Структура проекта:**
```
mira_bot/
├── bot_support/                 # Новый модуль для бота поддержки
│   ├── __init__.py
│   ├── main.py                  # Точка входа для support бота
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py             # Обработка /start
│   │   ├── messages.py          # Пересылка сообщений
│   │   └── admin_messages.py    # Обработка сообщений от админов
│   ├── services/
│   │   ├── __init__.py
│   │   ├── user_service.py      # Работа с support_users
│   │   ├── message_service.py   # Работа с support_messages
│   │   └── topic_service.py     # Создание/управление топиками
│   └── utils/
│       ├── __init__.py
│       └── formatters.py        # Форматирование карточек пользователя
│
├── database/
│   ├── models.py                # Добавить SupportUser, SupportMessage, SupportReview
│   └── repositories/
│       ├── support_user.py      # Новый репозиторий
│       ├── support_message.py   # Новый репозиторий
│       └── support_review.py    # Новый репозиторий
│
├── webapp/
│   ├── api/
│   │   └── routes/
│   │       ├── support.py       # API для раздела "Вопросы"
│   │       └── reviews.py       # API для раздела "Отзывы"
│   └── frontend/
│       └── admin.html           # Расширить интерфейс админки
│
└── config/
    └── settings.py              # Добавить настройки для бота поддержки
```

### 4.2. Зависимости
- **Telegram Bot API:** python-telegram-bot >= 20.0
- **Database ORM:** SQLAlchemy >= 2.0 (уже используется)
- **Async:** asyncio, asyncpg (уже используется)

### 4.3. Логирование
- Логировать все события:
  - Создание нового пользователя
  - Создание топика
  - Пересылка сообщений (user → admin, admin → user)
  - Ошибки API Telegram
- Уровень: INFO для обычных событий, ERROR для ошибок

### 4.4. Обработка ошибок
- **Если не удалось создать топик:**
  - Логировать ошибку
  - Уведомить администратора в основной чат группы
  - Отправить пользователю: "Произошла ошибка. Попробуйте позже."

- **Если пользователь заблокировал бота:**
  - Логировать событие
  - Пометить в БД флагом `is_bot_blocked = True`
  - Не пытаться отправить сообщение повторно

- **Если превышен лимит Telegram API:**
  - Retry с экспоненциальной задержкой (1s, 2s, 4s, 8s, 16s)
  - После 5 попыток — логировать ошибку и уведомить админа

### 4.5. Производительность
- **Database Indexes:** Обязательно создать индексы на:
  - `support_users.telegram_id` (unique)
  - `support_users.topic_id`
  - `support_messages.user_id`
  - `support_messages.created_at`
  - `support_reviews.created_at`

- **Connection Pool:**
  - Использовать существующий пул соединений из `config/settings.py`
  - Не создавать отдельный пул для support бота

- **Кэширование:**
  - Кэшировать `topic_id` по `telegram_id` в Redis (опционально)
  - TTL: 1 час

### 4.6. Безопасность
- **Проверка прав доступа:**
  - Только администраторы супергруппы могут отвечать на сообщения
  - Проверять `user.status` в `["creator", "administrator"]`

- **Защита от спама:**
  - Rate limiting: не более 10 сообщений в минуту от одного пользователя
  - Автоматическая блокировка при превышении лимита

- **Валидация данных:**
  - Проверять все входящие данные перед сохранением в БД
  - Экранировать HTML в сообщениях для админки

---

## 📊 Метрики успеха

### KPI для бота поддержки:
1. **Среднее время первого ответа:** < 2 часа
2. **Процент решённых обращений:** > 90%
3. **Uptime бота:** > 99.5%
4. **Ошибки API:** < 0.1%

### Метрики для админки:
1. **Время загрузки списка вопросов:** < 1 сек
2. **Время загрузки истории чата:** < 2 сек
3. **Доступность:** > 99%

---

## 🚀 Этапы разработки (см. TODO_SUPPORT_BOT.md)

1. **Этап 1:** База данных и репозитории (2-3 дня)
2. **Этап 2:** Бот поддержки — основной функционал (3-4 дня)
3. **Этап 3:** API для админки (2 дня)
4. **Этап 4:** UI админки (3-4 дня)
5. **Этап 5:** Тестирование и деплой (2 дня)
6. **Этап 6:** Документация и обучение (1 день)

**Общий срок:** 13-17 рабочих дней (3-3.5 недели)

---

## 📝 Дополнительные требования

### Интеграция с основным ботом @MiraDrug_bot
**Сценарий:**
- Пользователь пишет `/support` в основном боте @MiraDrug_bot
- Бот отвечает:
  ```
  Для связи с технической поддержкой перейди к боту:
  👉 @MiraDrugSupport_bot

  [Открыть бота поддержки]
  ```
- Кнопка ведёт на `https://t.me/MiraDrugSupport_bot?start=from_main_bot`

### Парсинг отзывов из топика #4
**Задача:**
- Автоматически парсить сообщения из топика #4 (Reviews)
- Формат сообщения (см. раздел 2.3)
- Регулярное выражение для парсинга:
  ```python
  pattern = r"""
  👤\s*От:\s*(@\w+)
  🎂\s*Возраст:\s*(\d+)
  ℹ️\s*О\s*себе:\s*(.+?)
  💬\s*Отзыв:\s*(.+?)
  📢\s*Разрешение:\s*(Да|Нет)
  """
  ```
- Сохранять в таблицу `support_reviews`

### Экспорт отзывов для лендинга
**Формат JSON:**
```json
{
  "reviews": [
    {
      "name": "Александр, 35 лет",
      "about": "Предприниматель, отец двоих детей",
      "text": "Мира помогла мне разобраться...",
      "date": "03.01.2026"
    },
    ...
  ]
}
```

**Endpoint:**
```
GET /api/public/reviews?permission=true&limit=10
```

---

## 🔒 Конфиденциальность

### Обработка персональных данных:
- **Согласие пользователя:** При первом использовании бота показывать согласие на обработку ПД
- **Хранение данных:**
  - Имя, фамилия, username — только в БД
  - История сообщений — удаляется через 90 дней после последнего сообщения (опционально)
- **Удаление данных:**
  - Команда `/delete_my_data` — удаляет все данные пользователя из БД
  - Топик в группе НЕ удаляется (для истории)

---

## 📞 Контакты

**Разработчик:** Claude Sonnet 4.5
**Заказчик:** @uspeshnyy
**Дата:** 03.01.2026

**Telegram для обсуждения:** @uspeshnyy

---

## ✅ Критерии приёмки

### Обязательные требования:
- [ ] Бот создаёт топик для нового пользователя
- [ ] Сообщения пользователя пересылаются в топик
- [ ] Сообщения админа пересылаются пользователю
- [ ] В админке отображается список обращений
- [ ] В админке отображается история чата
- [ ] В админке отображаются отзывы
- [ ] Настройки бота доступны в разделе "Конфиг"
- [ ] Логирование работает корректно
- [ ] Обработка ошибок работает корректно
- [ ] Документация создана и актуальна

### Желательные требования:
- [ ] Кэширование topic_id в Redis
- [ ] Экспорт отзывов в JSON
- [ ] Интеграция с основным ботом через `/support`
- [ ] Автоматический парсинг отзывов из топика #4
- [ ] Статистика по времени ответа

---

**Версия документа:** 1.0
**Последнее обновление:** 03.01.2026
**Статус:** ✅ Готово к разработке
